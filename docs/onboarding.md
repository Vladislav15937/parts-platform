# Подключение клиента

Порядок шагов от пустой ячейки до первой позиции, уехавшей на Дром.
Написан для того дня, когда клиент рядом и вспоминать некогда.

Пройден целиком на чистой ячейке дважды — от `docker compose up` до прайса,
забранного по постоянной ссылке. Первый прогон нашёл три ошибки, второй
прошёл без остановок. Замеры объёма — с прогонного арендатора «YARD Ткацкая»
(35 841 позиция, 440 машин).

Потом пройден **на боевой ячейке с первым живым клиентом** (Timeweb,
`parts.plus-parts.ru`, арендатор `yardt`) — и она нашла ошибку, которой оба
чистых прогона не видели: там приложение ходило одной ролью, владельцем,
а в бою — рабочей, и на разделении ролей перенос упал на первой же записи
в общий справочник. Разобрано в шаге 5. После починки на боевой ячейке
завелись 13 позиций, 5 машин, 2 поставки и 69 фотографий, прайс собрался
и отдался по постоянной ссылке с 13 объявлениями.

---

## До дня подключения

Три вещи не делаются за час, и без них день подключения упрётся в ожидание.

**Выгрузка из прежней системы — с колонками «Выгружать» и «Превью».** Обе
включаются в настройках таблицы товаров, и запрашивает выгрузку клиент сам.
Без «Выгружать» флаг публикации приходится угадывать — это либо объявления
на битые детали, либо пропавшие объявления на хорошие. Без «Превью» склад
переедет без единой фотографии, а на разборке продаёт фотография.

**Письмо Дрому про API защищённых сделок.** Ключ `drom-api` они выдают
по запросу, публичного описания протокола нет. Пока ключа нет, заказы
с площадки заводятся руками — механизм для этого готов, и при появлении
ключа поменяется только то, кто зовёт `registerMarketplaceOrder`.

**Письмо Дрому про прайс-лист.** Разбор нашего XML на их стороне настраивает
их техспециалист. Там живой человек, и это не день.

**Ключ синхронизации для дельт.** Запрашивается тем же обращением; один
на кабинет, действует на все прайс-листы. Но включение обновления по API
делается отдельно **каждому прайс-листу** — и при заведении нового
им надо написать снова. Это не разовый шаг подключения, а постоянная
обязанность: завели клиенту ещё один прайс — написали в поддержку.

Плюс от клиента: список филиалов и складов, коды ячеек (стеллажи, ряды),
список сотрудников с ролями.

---

## 1. Ячейка

Пилотных клиентов разложить по разным ячейкам. Десять компаний из одного
города общаются между собой: одна потерянная база — и сарафан работает против
вас необратимо.

```bash
cp .env.example .env          # секреты, домены, APP_CELL, APP_CELL_NUMBER
docker compose -f docker-compose.prod.yml up -d --build
ops/create-roles.sh           # рабочая роль: журналы приложению не принадлежат
docker compose -f docker-compose.prod.yml up -d app    # перезапуск под ней
ops/install-cron.sh           # бэкапы: ежедневно, проверка по понедельникам
```

**Заведение ролей — не необязательный шаг.** Без него приложение ходит в базу
владельцем всех таблиц, и «кто уронил цену» перестаёт быть вопросом
с ответом: журнал правится прямым SQL, а `REVOKE` владельца не ограничивает.
Ячейку с одним клиентом это не ломает ничем видимым — разница проявится
в тот день, когда журнал предъявляют как доказательство. Скрипт печатает
пять переменных, которые после него должны стоять в `.env`; заполнять их надо
все разом — имя рабочей роли без её пароля означает подключение ролью,
которой ещё нет, и приложение не поднимется вовсе.

**Если ячеек на машине больше одной**, файл окружения выбирается переменной
`ENV_FILE` — одинаково у всех трёх скриптов бэкапа: `ENV_FILE=.env.cell02
ops/backup.sh`. Позиционный аргумент у них разный (набор, схема), и держать
в голове, какой по счёту здесь env, — ровно тот способ однажды снять бэкап
одной ячейки, а проверить другой.

**Переопределить одну переменную на лету нельзя**: файл читается через
`set -a` и перебивает командную строку. `APP_CELL=cell02 ops/backup.sh` молча
уйдёт в ячейку из файла.

Проверить до того, как в ячейке появится клиент:

- `https://<домен>/` открывается **на настоящем домене**. На `localhost`
  Caddy выдаёт сертификат своего локального центра — браузер ему не верит,
  а без доверенного сертификата нет защищённого контекста, то есть нет
  ни камеры, ни сканера. Проверять приёмку с телефона на локальной ячейке
  бессмысленно: сломается не приёмка, а доверие к сертификату;
- тревога доходит: `docker stop <ячейка>-app-1`, через две минуты приходит
  сообщение в Telegram, `docker start` — приходит снятие;
- **правил в ячейке столько же, сколько в `ops/alerts.yml`.** Prometheus
  не перечитывает файл сам: правило, добавленное после подъёма контейнера,
  лежит смонтированным и не действует, а выкладка перезапускает приложение
  и до него не доходит. Ошибки при этом нет нигде — видно только сверкой:

  ```bash
  grep -c 'alert:' ops/alerts.yml
  docker compose -f docker-compose.prod.yml exec -T prometheus \
    wget -qO- localhost:9090/api/v1/rules | grep -o '"type":"alerting"' | wc -l
  ```

  Именно `grep -o … | wc -l`, а не `grep -c`: ответ приходит одной строкой,
  и счёт строк даёт единицу при любом числе правил — то есть ровно ту
  ошибку, ради которой сверку и делают.

  Разошлось — перечитать: `wget -qO- --post-data='' localhost:9090/-/reload`
  внутри того же контейнера;
- `ops/backup.sh && ops/verify-backup.sh` проходят целиком;
- в логе приложения при старте стоит **«Журналы защищены»**, а не «Журналы
  правятся прямым SQL». Второе означает, что ячейка работает одной ролью:
  запуск при этом не валится намеренно — в разработке одна роль законна, —
  и потому единственный признак снятой защиты живёт в одной строке лога;
- **провижининг снаружи не отвечает**: `curl -o /dev/null -w '%{http_code}\n'
  -X POST https://<домен>/api/provisioning/tenants` обязан дать 404.
  `PROVISIONING_ALLOW` ограничивает управляющий контур по адресам, и
  `0.0.0.0/0` в нём — это возможность завести схему у любого желающего,
  пока не кончится диск. Проверено попыткой, а не чтением конфигурации:
  правило живёт в Caddyfile, а значение — в `.env`, и разойтись им ничего
  не мешает.

Ячейка, на которой это не проверено, отличается от проверенной только тем,
что вы об этом не знаете.

---

## 2. Арендатор

```bash
docker compose -f docker-compose.prod.yml exec app \
  wget -qO- --header='Content-Type: application/json' \
    --post-data '{"token":"<APP_PROVISIONING_TOKEN>",
                  "companyCode":"yardt",
                  "companyName":"YARD Ткацкая",
                  "ownerLogin":"vladelec",
                  "ownerPassword":"<пароль>",
                  "ownerName":"Иван"}' \
    http://localhost:8080/api/provisioning/tenants
```

**Именно `wget`: `curl` в образе приложения нет.** Образ собран на JRE
без лишнего, и `curl` там не появится ради одной команды раз в жизни клиента.

**Изнутри контейнера, а не снаружи.** Управляющий контур закрыт по адресам,
и запрос с хоста через терминатор получает 404 — проверено прогоном.

Одна операция: запись в реестре, схема, миграции, владелец, филиал и первый
склад. Отвечает номером арендатора и именем схемы.

**Код компании клиент будет вводить при каждом входе** — короткий, латиницей,
без сюрпризов в раскладке. Позже он же станет поддоменом.

**Секрет провижининга выключается сразу после подключения** (пустое значение
в `.env`, перезапуск приложения). Забытый включённым, он даёт любому,
кто дошёл до управляющего контура, заводить схемы, пока не кончится диск.

---

## 3. Филиалы, склады, ячейки

Провижининг завёл один филиал и один склад — этого хватает, чтобы принять
деталь, но не хватает живому клиенту.

```
POST /api/organization/branches           филиал
POST /api/organization/warehouses         склад в филиале
POST /api/organization/warehouses/{id}/cells   ячейки списком
```

**Ячейки заводятся списком, а не по одной.** Стеллаж — это два-три десятка
адресов подряд, и по одному их не заведёт никто: коды уедут в примечание,
а поиск детали на полке вернётся к памяти кладовщика. Уже существующие
пропускаются, а не ломают запрос целиком.

**Кириллица в кодах ячеек проверяется здесь, а не на печати этикеток.**
Code128 не знает кириллицы вовсе. «А», «В», «Е», «К» переживают это — сканер
сводит их к латинице, — а «Б», «Г», «Д» двойника не имеют, и такой адрес
не отсканируется никогда. Экран этикеток такие адреса помечает и не даёт
выбрать. Переименовать десять полок в первый день дешевле, чем через месяц
объяснять кладовщику, почему сканер их не видит.

---

## 4. Сотрудники

```
POST /api/members    логин, пароль, имя, роль
```

Роли: `OWNER`, `MANAGER`, `STOREKEEPER`, `SELLER`, `VIEWER`.
Заводит сотрудников только владелец. Отчёты и выгрузки видит
владелец и менеджер: там зарплатная база смены и себестоимость.

Заводить до переноса не обязательно, но удобно: в день подключения продавцы
уже смогут посмотреть склад.

---

## 5. Перенос склада

Два файла из прежней системы — машины и товары:

```
POST /api/import/bazon
  donors=<выгрузка машин>.csv
  catalog=<выгрузка товаров>.csv
```

**Это CSV в windows-1251 с точкой с запятой — то, что отдаёт прежняя
система.** Не `.xlsx`. Таблица Excel, поданная сюда, — другой эндпоинт
(`/api/import/excel`, для произвольных таблиц). Ошибиться легко, и до прогона
этой инструкции ошибка стоила дорого: чужой формат читался как текст,
и часть строк доезжала до склада призрачными карточками «Без наименования».
Теперь такой файл отвергается по заголовку, но перепутать эндпоинты
всё равно значит потерять время.

Вместе с позициями переезжают поставки, доноры, резервы по складам и флаг
«Выгружать». Повтор безопасен: товар узнаётся по своему коду из выгрузки,
машина — по своему, поставка — по паре «тип и номер».

Замерено: 35 841 позиция и 440 машин — 59 секунд первый раз, 32 повторный.
Файл на 24,7 МБ проходит (предел 64 МБ).

**Категория-заглушка «Не разобрано» сеется миграцией, а не импортом — и это
выяснилось на первом боевом переносе.** Каждой позиции нужна категория,
выгрузка её не отдаёт, и все едут в «Не разобрано». Заводил её раньше сам
импортёр одним `INSERT` в общую схему `catalog` — а на боевой ячейке
приложение ходит рабочей ролью, которой `catalog` дан только на `SELECT`
(справочники пишет миграция, читают все). `SELECT` категории проходил,
`INSERT` отвечал «permission denied for table part_category», и весь перенос
уезжал «Внутренней ошибкой» ещё до первой позиции. Оба чистых прогона этого
не видели: там приложение ходило владельцем, которому писать в `catalog`
можно. Теперь строку сеет changeset `catalog/020`, импорт её только читает,
и рабочая роль в общую схему не пишет вовсе. На уже поднятой ячейке со старой
сборкой строку доводят разово владельцем — после чего changeset при деплое
увидит её и пройдёт мимо (`ON CONFLICT (path) DO NOTHING`):

```bash
docker compose -f docker-compose.prod.yml exec -T postgres \
  psql -U "$DB_USER" -d parts -v ON_ERROR_STOP=1 -c \
  "INSERT INTO catalog.part_category (name, slug, path, sort_order)
   VALUES ('Не разобрано','uncategorized','uncategorized'::ltree,9999)
   ON CONFLICT (path) DO NOTHING;"
```

**Повтор — обычное действие, а не авария.** Если ответ не пришёл или пришёл
ошибкой, повторяйте: ключ идемпотентности защищает от второй копии склада.

**После переноса импортёр сам доводит справочник и карточки.** Ничего
дополнительно запускать не нужно.

**Фотографии переносятся отдельно, пачками.** Импорт кладёт их в очередь
(в ответе — «фотографий в очереди»), а качает отдельный проход:

```
POST /api/import/bazon/photos?limit=200   перенести пачку
GET  /api/import/bazon/photos             сколько осталось и сколько не вышло
POST /api/import/bazon/photos/retry       вернуть неудачные в очередь
```

Пачками, потому что сотня тысяч файлов с чужого CDN — это часы, а запрос
столько не живёт. Нажимать, пока в очереди не станет пусто; прервать можно
в любой момент. Битые ссылки не останавливают проход — они копятся
в «не вышло», и после починки CDN их возвращают в очередь отдельной кнопкой.
Замерено: три снимка за секунду.

---

## 6. Разбор наименований

Открыть вкладку «Наименования» — там число нераспознанных написаний.

На переехавшем клиенте после переноса их было 1 020 под 11 182 карточками —
треть склада без вида детали, то есть без категории, то есть не уедет
на площадку. После расширения справочника и разбора руками осталось 225
написаний под 1 658 карточками.

**Сегодня та же выгрузка переносится сразу разобранной.** Прогон подключения
на чистом арендаторе (35 841 позиция, 68 секунд): 1 265 написаний, из них
сопоставлено 590, категорию получили 32 591 карточка. Нераспознанными
остаются 675 написаний под 3 250 карточками, и 420 из них держат
по одной-две позиции — это работа глазами по карточке, словарём
её не закрыть. Разбирать имеет смысл верхушку списка.

Что делать:

1. `POST /api/part-names/rematch` — пересопоставить по нынешнему справочнику.
   Справочник растёт с релизом, а написания клиента заведены раньше.
2. Разобрать сверху вниз: список отсортирован по числу позиций, и одно
   сопоставление правит сотни карточек разом (на прогоне «тросик ручного
   тормоза» → «Трос ручника» исправило 379).
   **Подсказки смотрят и на синонимы эталона, а не только на его имя** —
   то есть на живые написания, ради которых справочник и заведён. На том же
   «тросике ручного тормоза» верный «Трос ручника» теперь стоит первым
   (против имени эталона похожесть 0,296 — четвёртое место, против синонима
   «трос ручного тормоза» 0,826). Раньше подсказки предлагали «Колодки
   стояночного тормоза», и поле «Найти эталон» было основным путём;
   сейчас оно спасает там, где подсказки мимо, и ищет тоже по синонимам.
3. Смотреть на строку **«Станет»** перед нажатием. Она показывает, каким
   станет заголовок карточки. Разница между верным сопоставлением и ложью
   в заголовке видна только там: «тросик ручного тормоза» → «Трос ручника»
   верно, «Знак аварийной остановки» → «Набор инструментов» — нет, а обе
   кнопки выглядят одинаково. Сопоставление правит сотни карточек и назад
   не откатывается.
4. Что не определяется по написанию — оставить. «Стекло» бывает лобовым,
   задним, двери и форточкой; эталон отправил бы деталь в чужую категорию,
   а это хуже нераспознанного: нераспознанное видно на экране, неверно
   распознанное — нет.

**Не откладывать на после первой выгрузки.** Позиция без категории на площадку
не уедет, и обнаружится это по пустому прайсу, а не по сообщению об ошибке.

---

## 7. Выгрузка на Дром

Экран «Выгрузки», владелец или менеджер.

1. Завести кабинет площадки — формой «Завести выгрузку» на том же экране:
   площадка («Дром»), название (оно же различает несколько прайсов одной
   площадки) и что выгружаем — запчасти или колёса. Запросом это тоже можно
   (`POST /api/marketplace-accounts`), но с экрана быстрее и без разработчика.
2. Задать отбор. Готовых полей шесть — цена от и до, состояние, склады, виды
   деталей, марки, — а сверх них в блоке «Свои условия» доступны те же
   колонки, какими владелец отбирает склад: производитель, секция, заметка,
   сезон и диаметр у колёс, «пусто» и «не пусто». Условий можно поставить
   несколько.

   Пустое поле — «без ограничения», а не «ничего»: выгрузка, у которой
   не заполнили цену, отдаёт весь склад. **Смотреть на счётчик позиций
   перед сохранением.** Ноль показывается ошибкой, а не числом: пустой прайс
   площадка примет молча, и объявления пропадут вместе с накопленными
   просмотрами, за которые и платят.

   Полезная сверка на подключении: число из счётчика обязано совпасть
   с числом `<offer>` в скачанном прайсе.
3. Получить постоянную ссылку: `POST /api/marketplace-accounts/{id}/feed-url`.
   Она вида `/feeds/drom/{код}/{токен}.xml` и работает без входа — площадка
   забирает прайс сама, cookie у её сервера нет и не будет.
4. Передать ссылку техспециалисту Дрома.
5. **Ввести ключ синхронизации** — блок «Ключ синхронизации» на том же
   экране. Без него дельты не уходят вовсе, и заметить это нечем: очередь
   отметок разгребается, `publication_log` пуст, всё выглядит работающим.
   Ключ выдаёт поддержка площадки по обращению, в кабинете он нигде
   не показывается, вводится сырым — SHA-512 приложение считает само.

6. **Попросить включить обновление по API для этого прайс-листа.** Ключ
   синхронизации один на кабинет и действует на все прайсы, но обновление
   по API включают отдельно каждому — руками, по обращению через
   `baza.drom.ru/question/other/`. Подтверждено их поддержкой дословно:
   «при загрузке нового прайс-листа вам нужно будет сообщить нам, чтобы мы
   включили обновление по API для этого прайс-листа».

   Шаг легко пропустить, потому что он ничего не ломает заметно: дельты
   уходят, приложение пишет «отправлено», `publication_log` зелёный —
   а на сайте цена и остаток стоят прежние до следующего полного забора.
7. **Включить автообновление по ссылке в кабинете клиента**: «Обновление
   прайс-листа» → «Автоматически» → «Периодически обновлять прайс-лист
   по ссылке ниже». По умолчанию там стоит «Не обновлять», и с этой
   настройкой **новые товары не появятся вовсе** — API их не заводит,
   см. ниже.

**Прайсов на одну площадку может быть несколько**, и различаются они отбором:
у живого клиента их пять — «новые», «низкая», «средняя», «высокая», «очень
высокая», — у каждого свой прайс-лист в кабинете и своя цена размещения.

**Смена ссылки останавливает выгрузку.** Старая перестаёт работать сразу,
новую в кабинет площадки прописывает их техспециалист руками. Поэтому это
отдельное действие, а не побочный эффект сохранения настроек.

Замерено на прогоне подключения (35 841 позиция): полный прайс — 21,5 МБ
за 1,3 секунды, в нём 35 831 объявление. Десяти не хватило до счётчика
по делу: у них нулевая цена, и в прайс такие не идут — ноль в выгрузке
прежней системы стоит там, где поле не заполняли, а `<price>0.00</price>` —
это публичное обещание отдать деталь даром.

**Снимков в этом файле пока нет, и это видно только сверкой.** Фотографии
переносятся отдельным проходом (шаг 5), и пока он не прошёл, объявления
уедут без картинок — при том что на разборке продаёт фотография. Проверять
надо не «прайс собрался», а число снимков в нём:

```bash
curl -s "$URL" | grep -o '<photo>' | wc -l
```

Элемент называется `<photo>`, а не `<picture>` — на этом легко ошибиться
и получить честный ноль при полном прайсе снимков. И считать надо
`grep -o … | wc -l`: `grep -c` считает строки, а прайс — одна длинная.

### Новые товары заводит только полный прайс, а не API

Их поддержка отвечает на это прямо: «Функционал API служит только для
обновления товаров из выгруженных прайс-листов в режиме онлайн. Выгружать
новые объявления таким образом не получится».

Отсюда разделение труда, и его надо понимать до подключения:

| Что происходит | Чем доезжает | Когда |
|---|---|---|
| принята новая деталь | полный прайс | при следующем заборе |
| изменилась цена, остаток, снятие с продажи | дельта по API | секунды |
| деталь продана | дельта по API | секунды |

Забор полного прайса по ссылке — **раз в трое суток**, раз в сутки при
платном позиционировании, в 00:00 по Владивостоку. Значит новая деталь
появляется на сайте не сразу, и это не наш недостаток, а устройство
площадки. Ускоряют это двумя способами, оба на стороне клиента: залить
файл руками через кабинет или отправить его письмом на выделенный адрес
прайс-листа — там ограничений по частоте нет.

**Ключ синхронизации** (`PUT /api/marketplace-accounts/{id}/credentials`)
нужен для дельт по прайс-листам — это он, а не ключ API защищённых сделок.
Ключи разные: `drom-api` для приёма заказов запрашивается отдельно
(см. «Что запросить заранее»), и путать их не надо. Ключ синхронизации
выдаёт поддержка по обращению, он один на кабинет и выглядит как UUID;
в кабинете площадки его нигде не показывают, так что хранить надо там,
где хранят секреты.

Вводится он **сырым**: `DromSyncClient.authHash` берёт от него SHA-512 сам,
хешировать руками не надо. Шифруется и наружу не отдаётся ни одним
эндпоинтом — отсутствие метода «показать» это часть защиты. Без
`APP_CRYPTO_KEY` запись ключа отвечает ошибкой, а не пишет открытым текстом
молча.

---

## 8. Приёмка с телефона

Клиент открывает домен на телефоне и ставит на экран как приложение.
Домен — настоящий: на локальном сертификате камера не работает вовсе.

Проверить в первый же день, стоя на складе:

- вход работает и запоминается — приложение обязано открываться без связи;
- сканер находит ячейку по этикетке;
- фотография уходит в хранилище (она идёт мимо приложения, прямо в S3);
- деталь, занесённая в ангаре без связи, доезжает после возвращения связи.

**«Код не найден в справочниках» при сканировании — это чаще всего пустой
справочник ячеек, а не поломка сканера.** Сканер приёмки ищет прочитанный код
среди ячеек текущего склада и среди VIN машин — и больше нигде: штрихкод самой
детали он не смотрит вовсе (деталь тут ещё создают, её нет). Провижининг ячеек
не заводит, а перенос из выгрузки заводит склады и машины, но не полки —
и VIN у перенесённых машин пуст. Пока ячеек ноль, сканеру нечего сопоставлять,
и любой поднесённый штрихкод возвращает «не найден»: сканер при этом исправно
читает и исправно ничего не находит. Значит приёмку со сканером проверяют
уже после шага 3, с заведёнными ячейками. Всплыло на первом боевом клиенте
ровно так — сканер декодировал код, а в справочнике полок было пусто.

**Очередь отправки — единственный путь работы к серверу.** Экран кладёт запись
и считает работу сделанной. Если что-то не доехало, оно лежит во вкладке
«Очередь» с пометкой, а не пропадает.

---

## 9. Первая неделя

Смотреть три вещи:

**Вкладка «Наименования».** Число должно падать, а не расти. Растёт — значит
приёмщики вводят написания, которых нет в справочнике, и их надо разбирать
по ходу, а не копить.

**Вкладка «Доставка».** Событие, брошенное роботом после пяти попыток, ждёт
человека. Пусто — хорошо.

**Тревоги в Telegram.** Молчание — это либо всё хорошо, либо тревоги
не настроены. Проверяется остановкой приложения на две минуты, и лучше
проверить, чем предполагать.

И один вопрос клиенту в конце недели: **заносится ли деталь быстрее, чем
в прежней системе.** Это главный продуктовый показатель, и меряется он
секундомером на складе, а не ощущением.

---

## Чего в пилоте не будет

**Фискализации по 54-ФЗ.** Начинается с договора с оператором и регистрации
кассы, а не с кода. Что готово и чего не делать раньше времени —
`docs/fiscal-54fz.md`.

**Авито.** Генератор фида есть, доводить его сейчас не нужно — решение
владельца продукта. Выгрузка на первое время только Дром.

**Биллинга.** На двух клиентах счёт выставляется руками.

**Интернет-магазина на поддомене.** Недели 17–24 по плану.
