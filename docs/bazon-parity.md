# Паритет с Bazon: карта функционала и пробелы

> **Три документа про Bazon.** Карта функционала — что у них есть (**вы здесь**) ·
> [Модель данных](bazon-data-model.md) — как устроено ·
> [Сравнение архитектур](bazon-vs-us.md) — где кто сильнее.
> У всех трёх одна оговорка: их систему видно только снаружи, всё про её
> устройство — вывод, а не факт.

Снято с кабинета `yardt.baz-on.ru` (компания «YARD Ткацкая», ID 2226) 29.07.2026,
второй проход 30.07.2026.
Масштаб на момент обхода: 35 846 товаров, 221 шина/диск, 440 доноров,
593 клиента, 84 активные сделки, 3 464 возврата, 15 946 платежей.

Второй проход уточнил три места: денежное поле у донора (§0), полный состав
полей шин и дисков (§1), выгрузку справочника наименований при миграции (§11).
Разделы «Выгрузки» и «Пользователи и права» остались под замком, онлайн-кассы
по-прежнему требуют роли кассира — §8 в силе.

Назначение — не описание чужого продукта, а **список сценариев, которые должна
закрывать наша система**, с отметкой, что из этого уже есть в схеме и коде.
Повторяем сценарии и операции, не вёрстку и не содержимое чужого каталога.

Статусы: `нет` — не начато · `схема` — таблицы есть, кода нет ·
`частично` — код есть, неполный · `есть` — работает.

---

## 0. Главное открытие: это не та бизнес-модель, под которую написана наша схема

Наша схема исходит из того, что разборщик покупает битую машину, снимает с неё
запчасти и разносит затраты по донору на детали. У YARD другая модель:

**Машины и запчасти приходят контейнерами из Японии.** У каждого донора есть
поставка вида «Контейнер №17 | 01.07.2026 | Onteco 6», статус-локация
(«В Барнаул»), аукционный лист среди фотографий. У товара — та же поставка,
причём **контрактные запчасти приходят контейнером напрямую, без донора**
(«Контракт. Без пробега по РФ»).

Из этого следуют три вещи:

1. Нужна сущность **поставки/контейнера** — её в нашей схеме нет вообще.
   Это не поле, а узел: к нему привязаны и доноры, и товары, и логистика.
2. Донор перестаёт быть единственным источником товара. `part.donor_id`
   у нас уже nullable, но привязки к поставке нет.
3. Статус донора — это **локация в логистической цепочке**, а не стадия разбора.

**Деньги по донору: одно поле, без разнесения.** У донора 21 активная колонка,
и среди них денежных нет. Но в **неактивных** лежит **«Стоимость»** — рядом
с «Создан», «Изменён», «Количество фото» и «Ссылка на видео».

> Поправка от 30.07.2026. В первой версии этого раздела было сказано, что
> денежных полей у донора нет вовсе, «проверено через настройки таблицы».
> Проверены были только активные колонки, неактивные не просмотрены.
> Заполнено ли поле данными, неизвестно: включение колонки меняет настройки
> таблицы арендатора, и при повторном обходе её не трогали.

Дифференциатор от этого не исчезает, но сжимается. У них — одна сумма на
машину. У нас `donor_cost` как журнал затрат (несколько записей с типами),
`cost_allocation_method` — разнесение этих затрат на снятые запчасти,
и `deal_item.cost_price_snapshot` — фиксация себестоимости в момент продажи.
Разница не «есть против нет», а «журнал с разнесением против одного поля».
Строить позиционирование на «у них этого нет» нельзя; на «у них это
не считается по деталям» — можно.

Оговорка остаётся: учётка не админская, часть разделов закрыта (см. §8),
и полная финансовая картина могла быть скрыта правами.

---

## 1. Товары и услуги (`/catalog/`)

### Три типа номенклатуры

Верхние вкладки: **Автозапчасти** · **Шины и диски** · **Услуги**.
У нас есть только первый.

| Тип | Особенности |
|---|---|
| Автозапчасти | основной поток, 35 846 позиций |
| Шины и диски | **два подтипа в одной таблице**: колонка «Товар» = Шина / Диск. Объединяет их **номер комплекта**. Полный состав полей — ниже |
| Услуги | только номер, наименование и цена. **Единственный тип без склада**: у шин колонки складов есть, у услуг нет. У YARD их две: Доставка 300, Упаковка 200 |

#### Полный состав полей шин и дисков

Снят из «Настроить таблицу» 30.07.2026. Первая версия раздела перечисляла
только шинные поля и ни одного дискового.

Активные (26): Склады · Номер товара · Превью · **Номер комплекта** ·
**Товар** (Шина/Диск) · Диаметр · Тип шины · Ширина шины · Тип маркировки ·
Тип протектора · Тип конструкции · Высота шины · Износ · Производитель шины ·
Модель шины · Сезон · Год производства · **Тип диска** · **Ширина диска** ·
**Вылет** · **Сверловка** · **Диаметр ЦО** · **Производитель диска** ·
**Модель диска** · Номер производителя · Цена · Комментарий · Заметка · Секция

Неактивные (17): Создан · Изменён · Кто изменил · Поставка · Наименование ·
Состояние · **RunFlat** · **Легкогрузовая (LT)** · **Индекс скорости** ·
**Индекс нагрузки** · Номер донора · Выгружать · Количество фото ·
Старые данные · Ст. баркод · Цена изменена в · Кто изменил цену

У диска шинные поля просто пустые — в выгрузке видно, что у позиций «Диск»
заполнен только диаметр. Это не два справочника, а один с необязательными
полями обоих видов.

Оценка «2 нед» в §9 занижена: это 43 поля и подтип, а не «отдельный набор
атрибутов».

### Два режима таблицы

- **Продажа** — таблица работает как касса: клик по строке даёт степпер
  количества в колонке склада, позиции набираются в «Список» вверху.
- **Редактирование** — чекбоксы для массовых операций, номер становится
  ссылкой на карточку, появляется колонка «Цена».

### Полный состав полей товара

Снят из «Настроить таблицу» — это фактически схема `part`.

Активные: Склады · Номер товара · Превью · Запчасть · Оценка состояния ·
Марка · Модель · Поколение донора · Рестайлинг донора · Кузов · Двигатель ·
Год выпуска · Передний/Задний · Левый/Правый · Номер донора · Цена ·
Установка · Цвет · Комментарий · Производитель · Номер производителя ·
Кросс-номера · Заметка · Маркировка · Секция

Неактивные: Создан · Изменён · Кто изменил · Поставка · Наименование ·
Состояние · Текстовый блок · Комплектация · Выгружать · Количество фото ·
Видео · Старые данные · Ст. баркод · Цена изменена в · Кто изменил цену ·
Габариты товара · Вес товара · Габариты товара в упаковке · Вес товара в упаковке

Чего не было у нас на момент обхода: поставка, установка, секция, три
отдельных текстовых поля, габариты и вес отдельно для товара и упаковки,
аудит цены, «старые данные» под миграцию, количество фото и видео.

**Закрыто всё, кроме одного.** В `part` есть `supply_id`,
`installation_price`, `section`, три текстовых поля (`description`, `note`,
`text_block`), габариты и оба веса, `price_changed_at` и `price_changed_by`,
`legacy_code` с `legacy_data`, `video_url` и счётчик снимков. Витрина склада
сверена с их таблицей товаров колонка в колонку, и список заголовков стережёт
тест. Не заведён **`legacy_barcode`** («ст. баркод»): переехавшему клиенту
он не понадобился ни разу, а колонка «на будущее» — это пустое поле
у всех.

### Автогенерация наименования

Заголовок карточки собирается автоматически:
`Блок подрулевых переключателей Toyota Wish ZNE10 1ZZFE 2006 (б/у) 8414012530`
— вид детали + марка + модель + кузов + двигатель + год + состояние + OEM.
Продавец не пишет название руками. У нас `part.title` заполняется вручную.

### Оценка состояния — фиксированные градации

Четыре значения с описаниями: **Как новая** · **Без дефектов** ·
**С дефектами** · **Требует ремонт**. Подпись в интерфейсе:
«данные по оценке передаются на рекламные площадки».

У нас `part.quality_grade` — свободный текст. Нужен фиксированный справочник
и маппинг в фиды.

### Карточка товара

Две вкладки: **Описание** и **Донор задан** (вторая показывает машину целиком —
125–157 запчастей с неё, фото с аукциона, «Посмотреть» ведёт в список).

Меню карточки: **История изменений** · **Печать штрих-кода** · **Скачать все фото**.
История изменений сама разделена на **Редактирование** (аудит полей) и
**Движение товара**.

**Движение товара оформляется документами**, а не плоскими записями:
колонки Дата/Время · Документ/Склад · Статус · Кол-во · Цена · Поступление ·
Поставка; пример строки — «Поступление 7163 / Ткацкая / Выполнен / 1».
У нас `stock_movement` с `ref_type`/`ref_id` — движение есть, документа со
статусом над ним нет.

### Инструменты списка

| Инструмент | Что делает |
|---|---|
| Поиск по номеру | по номеру товара и OEM |
| Поиск по применимости | единая строка «марка, модель, кузов или двигатель» + пошаговый выбор → «Показать запчасти». Главный экран продавца |
| **Проценка товаров** | рыночная переоценка: гистограмма распределения цен по номеру и кросс-номерам, ваша цена — маркером на ней. Фильтры по дате изменения цены, виду запчасти, производителю, тумблер «искать с учётом кросс-номеров». Помечено «тестовый режим» |
| Добавить фото к товарам | массовая привязка фотографий |
| Применимость подробно | |
| Архив товаров | |
| Настройки списка | отображать зарезервированные · отображать отсутствующие · поиск с учётом применимости · мои склады · настроить таблицу |
| Скачать таблицу | экспорт, есть в каждом разделе |

**Про «Проценку» отдельно.** Она работает на агрегате цен по всем клиентам
Bazon. В одиночку это не воспроизводится: нужна накопленная база. Это их ров,
и планировать паритет по этой фиче на старте бессмысленно — она появляется
как следствие числа клиентов, а не как задача в спринте.

---

## 2. Сделки (`/sales/`)

Три представления: **Списком** · **По товарам** · **По статусам**.
Воронка слева: Новые (3) · В работе (84) · Выданные · Отменённые · Все сделки.

**По товарам** — те же сделки, развёрнутые до позиций (561 товар в 84 сделках):
Номер сделки · Дата · Номер товара · Наименование · Количество · Цена ·
Склад выдачи · **Статус товара** · Статус сделки. Статус есть и у позиции
(«Зарезервирован»), не только у документа.

**По статусам** — канбан с фильтрами по складу выдачи, источнику и
ответственному. Колонки на момент обхода:

| Колонка | Шт. |
|---|---|
| Новая сделка | 3 |
| **Истек срок** | **62** |
| Ждет оплаты | 13 |
| Частично оплачен | 6 |
| Готов к выдаче | 3 |

Колонка «Истек срок» — самая большая, и это принципиально: **резерв не
снимается автоматически**. По истечении срока сделка переезжает в отдельную
колонку и ждёт решения менеджера. В нашей модели (`docs/domain-model.md`, §3.7)
резерв истекает фоновой задачей, событие уходит в шину и объявление
возвращается на площадки. Это другое поведение, и здешнее выглядит
осмысленнее: 62 зависших резерва — это не мусор, а очередь на обзвон.
Автоснятие превратило бы её в невидимую потерю.

Колонки списка: Номер/дата · Клиент · Склад выдачи · Комментарий · Сумма ·
Статус · Комментарий · Источник · Ответственный.

Статусы: **Резерв** (с датой «до 02 авг») · **Готов к выдаче** (+ метка
«Оплачено») · **Выдан** · **Отменён**.

### Карточка сделки

URL — `/client/2/sales/67969/`: **сделка живёт внутри клиента**, а не сама по себе.
Вкладки клиента: Клиент · Сделки · Возвраты · Платежи. Слева — все сделки
этого клиента.

Действия: Выдать · Отменить сделку · Внести оплату · **Пробить чек** ·
печать · ссылка.

Поля: Сумма сделки · Оплачено · Склад выдачи · Комментарий к сделке ·
Комментарий к доставке · Ответственный · Источник сделки ·
**Фотографии и видео к сделке**.

Позиции сгруппированы по складу. «Подробнее» разворачивает их в полную таблицу
товара с колонками «Кол-во в документе» и «Цена в документе».

### История документа

Правая колонка — человекочитаемая лента:

> Саповатов Никита, 17:34 — Сделка создана и зарезервирована. Добавлено позиций 102214, 102213, 103782…
> Саповатов Никита, 17:43 — Перенесено в новую сделку 67971: 102214 Моторчик привода заслонок печки…

Отсюда две вещи, которых у нас нет: **перенос позиций между сделками** как
операция и **повествовательный журнал** по документу. `audit_log` у нас пишет
таблицы и поля — это не то же самое, из него такую ленту не собрать без
отдельного слоя событий с человекочитаемым текстом.

---

## 3. Возвраты (`/returns/`)

Отдельные документы с собственной нумерацией, 3 464 штуки.
Колонки: Номер/дата · Клиент · По сделке · Склад возврата · Сумма · Статус
(Выполнен + дата) · Комментарий (причина: «Не рабочий», «Не подошёл»,
«Не в нем дело», «Сказали не…»).

У нас `deal_return` есть, но это строка, а не документ со статусом и складом
возврата.

---

## 4. Автомобили-доноры (`/references/carsrc/`)

440 машин. Режимы: Справочник / Редактирование.

Активные поля (21): Номер донора · Превью · Поставка · Марка · Модель ·
Поколение · Рестайлинг · Кузов · Двигатель · Год выпуска · Цвет · Статус ·
Описание · Руль · Привод · Тип КПП · Модель КПП · VIN · Комплектация ·
Пробег · Заметка.

Неактивные (5): Создан · Изменён · **Стоимость** · Количество фото ·
**Ссылка на видео**. Про «Стоимость» — см. поправку в §0.

Номер донора вида `418 (1417)` — внутренний номер плюс код в скобках.
Цвет — с заводским кодом: «Золотистый (4T8)», «Серебро (1C0)».
КПП — с моделью агрегата: «Вариатор, K112F-01», «АКПП, U341E-01A».
Комплектация — заводской код: `5271417`.

Чего нет у нас **и сейчас**: у донора одно текстовое поле `note` вместо двух
(описание отдельно от заметки) и нет ссылки на видео. Не заводилось
намеренно: у переехавшего клиента заметка заполнена и работает как опознание
машины («Синий маркер!!!»), а второго поля никто не просил. Остальное
закрыто миграцией `tenant/010`: поставка, локация-статус, руль, привод, тип
и модель КПП, код цвета, комплектация — и всё это теперь **видно
в карточке позиции**, на вкладке «Донор»: по телефону спрашивают именно руль,
коробку и привод.

Чего нет у них: **разнесения затрат по донору на снятые запчасти**. Само поле
стоимости у донора есть, но одно и без распределения — см. поправку в §0.

---

## 5. Клиенты (`/clients/`)

593 клиента. Колонки: ID · Тип клиента (Физ. лицо / Юр. лицо) · ФИО или
название · Телефон · Эл. почта · Примечание · Заметка · **Баланс** · ИНН ·
Название организации.

Карточка: основная информация, блок «Данные юридического лица» (тумблер),
и справа — **лицевой счёт**: «На счету» / «В резерве» + кнопка
**Пополнить / Списать**.

Сквозной паттерн интерфейса, повторяющийся везде:
**Примечание** — видно клиентам, выводится при печати накладных;
**Заметка** — видно только вам, нигде не выводится.

Отдельные клиенты-каналы: «Drom Защищенная сделка», «Авито доставка»,
«Частное лицо», «На списание». То есть заявки с площадок и служебные операции
оформляются на псевдоклиентов.

Лицевого счёта клиента у нас нет вообще — ни таблицы, ни понятия.

---

## 6. Платежи

Реестр: Все · Приходные · Расходные. Колонки: ID · Дата · Клиент ·
Тип документа · Номер документа · Сумма · Источник · Статус (Проведён) ·
Комментарий. Второй раздел — **Движения по счёту**.

Итог по компании на момент обхода: 15 946 платежей на 61 858 199 ₽,
приход 64 351 882, расход 2 493 683.

**Источники платежей** — справочник с типом источника:

| Источник | Тип |
|---|---|
| Drom Защищенная сделка | не указан |
| Авито доставка | не указан |
| В долг | не указан |
| ККМ | наличный расчёт |
| Карта Сбер | не указан |
| Карта Т-Банк | наличный расчёт |
| р/с Альфа банк | расчётный счёт |
| р/с Левобережный | расчётный счёт |
| ЮКасса | интернет-эквайринг (архивный) |

У нас `payment` привязан к сделке и имеет `payment_type` строкой. Здесь платежи —
самостоятельный модуль (касса) с приходом и расходом, справочником источников
и движениями по лицевому счёту клиента.

---

## 7. Настройки (`/service/`)

- **Биллинг**: «Bazon оплачен до 31 июля 2026»
- **X-BAZ.ru — управление магазином**: интернет-магазин как отдельный продукт
- **Настройка рабочего места** (привязана к компьютеру, не к пользователю):
  сканер штрих-кодов · принтер этикеток · склад выдачи по умолчанию ·
  касса по умолчанию
- **Онлайн-кассы**
- **Выгрузки** — под замком, не открылось (см. §8)
- **Общие настройки**:
  - **Склады** — 3 склада (Ткацкая ID 01, 54 YARD ID 03, Советской армии ID 02 —
    отключён), у каждого «адресное хранение» и **маркировка полок**.
    Добавление и отключение складов — только через поддержку
  - **Срок резервирования** — 3 дня по умолчанию для всех новых сделок,
    меняется в самой сделке; по истечении менеджер видит пометку «Истёк срок»
  - **Источники платежей** и **источники сделок** (Авито, Сайт)
- **Справочник наименований** — см. ниже
- Пользователи и права — под замком
- AeroAdmin для удалённого подключения поддержки
- Лента новостей продукта и реферальная программа «2 месяца бесплатно»

### Приложение-агент для ПК — то, чего нет в нашей архитектуре вообще

Отдельное Windows-приложение (v 3.1.0.24), установленное на каждом рабочем
месте и зарегистрированное в системе по имени компьютера. Что оно связывает:

| Устройство | Пример из кабинета |
|---|---|
| Принтер штрих-кодов | `Zebra GK420t - ZPL`, `ZDesigner GK420t` |
| Сканер штрих-кодов | `COM4` |
| Онлайн-касса | `АТОЛ FPrint-22ПТК, №00106309877560, версия 10.10.7.0, ФИСК.` |
| Папка на устройстве ↔ папка в Bazon | `C:\Users\Илья\Desktop\Bazon Фото` → «Илья Чеботко» |

Из браузера ничего из этого недостижимо: ZPL на этикеточный принтер,
COM-порт сканера, драйвер фискального регистратора. Агент — обязательный
компонент, а не удобство.

В нашем архитектурном документе (§3, «Про фронтенд») выбор сделан в пользу
PWA, и он закрывает мобильную приёмку. Но десктопное рабочее место кассира
и приёмщика он не закрывает: **нужен ещё один компонент, которого нет ни в
стеке, ни в плане** — локальный агент под Windows. Это не строчка в бэклоге,
а отдельный подпроект со своим циклом обновления и поддержкой железа.

### Печатные формы

Два раздела: «Документы для печати» и «Реквизиты юр. лица».

**Реквизиты задаются на каждый склад отдельно**, и это разные юрлица:
54 YARD — «ИП Иванов И.В., Омск, проспект Дзержинского 102»,
Ткацкая — «ИП Санин Д.В., Барнаул, Ткацкая 626». Используются в накладных
сделок, возвратов, **списаний, перемещений** и в товарных чеках. Отсюда же
следует, что документы списания и перемещения в системе есть.

Настройки товарного чека и накладной: печать по умолчанию, ориентация,
дополнительный текст (у YARD — гарантия 14 календарных дней с условиями),
размер шрифта, где показывать доп. текст, поле подписи для клиента, поле
подписи выдавшего товар, **пометка об НДС** («в том числе НДС 5% — ### руб.»),
логотип. У накладной в возврате — свой независимый набор тех же настроек.

### Папки с фотографиями

Папки заведены **по устройствам сотрудников**: «iPhone 11 Илья»,
«samsung SM Кирюха», «Danil Chashnikov 11 IPhone», «DESKTOP-O2668BF»,
«- Загруженные вручную -». Отдельно — корзина «Удалённые фотографии».

Рабочий процесс двухшаговый: сотрудник заливает пачку снимков со своего
телефона или ПК в свою папку, дальше через «Добавить фото к товарам» они
привязываются к позициям. Фотограф и приёмщик — разные люди и разные моменты
времени.

### Уведомления

Разделены на уведомления **для менеджеров** (эл. почта, в браузере) и
**для клиентов** (эл. почта, SMS). Триггеры: создание новой сделки,
получение оплаты от клиента. Отдельно — адрес для счетов от самого Bazon.

Клиентские уведомления по SMS у нас не заложены нигде.

### Справочник наименований — ключевая находка

2 259 наименований арендатора, сопоставленных с **эталонным справочником**
Bazon. Колонки: Создан · Наименование запчасти (как ввёл клиент) ·
Эталонное наименование · Категория. Вкладки: Все · Сопоставленные ·
**Несопоставленные (71)**. Инструменты: «Поиск дублей», «Создать наименование»,
тумблер «Использовать справочник эталонов». Несопоставленные помечены красным
«Не распознано».

Это ровно наша архитектура «локальный слой арендатора → общий каталог», но с
явным слоем сопоставления и процессом ручной доработки нераспознанного.
На момент обхода у нас были `catalog.part_kind` с синонимами
и `part.part_kind_id`, но не было ни таблицы соответствий арендатора,
ни статуса «не распознано», ни экрана разбора.

**Сделано целиком.** `part_name` хранит написание клиента с `match_status`
и счётчиком использований, экран «Наименования» показывает нераспознанные
по числу карточек за ними, а сопоставление правит и уже заведённые позиции.
На живом складе это дало 34 183 позиции с видом детали из 35 841. Два
отличия от их решения, оба намеренные: подсказки считают похожесть
по синонимам, а не по имени эталона (иначе верный эталон не попадает
в тройку), и сопоставление применяется вторым нажатием — одно нажатие правит
до трёхсот карточек, и назад это не откатывается ничем, кроме бэкапа.

Из новостей продукта видно направление развития: перемещение между складами
с телефона (27.07), распознавание номера производителя и маркировки камерой
с переносом текста в систему (17.06).

---

## 8. Чего не удалось посмотреть

Учётная запись, под которой шёл обход, не администраторская:
«Только просмотр — недостаточно прав для редактирования».

| Раздел | Причина | Почему это важно |
|---|---|---|
| **Выгрузки** | под замком, не открывается | Это Авито и Дром — раздел 7 нашего архитектурного документа и главная причина, по которой клиенты остаются. Без него карта неполна ровно в самом дорогом месте |
| **Пользователи и права** | под замком | Ролевая модель; у нас `tenant_member.role` без содержания |
| **Онлайн-кассы** | «настройка касс и налогообложения недоступна, попросите сделать вас кассиром» | Роль кассира в ролевой модели выделена отдельно. Сама настройка налогообложения не видна |
| **X-BAZ.ru** (интернет-магазин) | «недостаточно прав для перехода в этот раздел» | Отдельный продукт поверх учётной системы |
| Мобильное приложение | недоступно с десктопа | Приёмка, видео 30 сек, OCR номера с запчасти, перемещение между складами |
| Отчёты и аналитика | **раздела в меню нет** | Всё меню — 6 пунктов: товары, сделки, возвраты, доноры, клиенты, настройки. Либо отчётов нет как раздела, либо они в закрытых частях |

---

## 9. Сводка пробелов

Приоритет: **1** — без этого продукт не работает у этого типа клиента,
**2** — заметно для пользователя, **3** — можно позже.

> **Что из этого закрыто на 9 августа 2026.** Все пробелы приоритета 1
> и большинство второго. Закрыты: поставки и контейнеры, остаток по нескольким
> складам, складские документы, товар без донора, справочник наименований
> с экраном нераспознанных, градации состояния, автогенерация наименования,
> шины и диски (три вида товара, включая колесо в сборе), услуги в сделке,
> лицевой счёт клиента, платежи со справочником источников, возврат
> документом, перенос позиций между сделками, история документа и карточки,
> настраиваемые таблицы с выгрузкой, габариты и вес, аудит цены, адресное
> хранение с печатью этикеток, просроченный резерв решением менеджера,
> статус на уровне позиции сделки, поля миграции.
>
> **Осталось незакрытым**, и каждое — решение, а не забывчивость:
>
> | Пробел | Почему не сделано |
> |---|---|
> | Локальный агент под Windows | Печатает браузер: команды ZPL привязали бы к модели принтера, а у первых клиентов они разные. Ценой — размер этикетки выставляется в диалоге печати один раз |
> | Рабочее место: привязка сканера, принтера и склада к компьютеру | Склад по умолчанию отвергнут осознанно: подставленный не тот склад — тихая ошибка, деталь ищут на полке и не находят |
> | Реквизиты юрлица по складам, печатные формы чека и накладной | Понадобится с первым клиентом, у которого несколько ИП. Пока не заявлено ни одним |
> | Фотопоток по устройствам сотрудников | Досъёмка из карточки закрывает главный случай; массовая привязка «фотограф отдельно» — работа под спрос |
> | Уведомления по email и SMS | Провайдер рассылок — это договор и деньги. Вместо них ссылка клиенту на его сделку, работающая в любом мессенджере |
> | Фото и видео к сделке | Не спрашивали |
> | Архив товаров | Не спрашивали; проданное и списанное со склада и так уходит из списков |
> | Одна активная сессия с перехватом | Мелочь; заметна только при разборе «кто это сделал» |
> | Проценка по рынку | Не планировать: работает на агрегате по всем клиентам, следствие масштаба |

| П | Пробел | Модуль | Оценка | Обоснование |
|---|---|---|---|---|
| 1 | Сущность поставки/контейнера, привязка доноров и товаров | intake | 1–2 нед | Вся товарная цепочка YARD построена на контейнерах; без этого не сходится происхождение товара |
| 1 | Остаток по нескольким складам у одной позиции | inventory | 1–2 нед | У нас деталь привязана к одной ячейке; в списке склады идут колонками с остатком в каждой |
| 1 | Складские документы (Поступление, Перемещение, Списание) со статусом над журналом движений | inventory | 2 нед | Движение показывается пользователю как документ, а не как строка журнала |
| 1 | Товар без донора: контрактные запчасти из поставки | intake | входит в поставки | |
| 1 | Справочник наименований арендатора ↔ эталонный, экран нераспознанных | catalog | 2 нед | Без сопоставления не работают ни поиск, ни выгрузки |
| 1 | Фиксированные градации оценки состояния + передача на площадки | inventory | 3 дня | Свободный `quality_grade` не ляжет в фид |
| 1 | Автогенерация наименования товара | inventory | 1 нед | Продавец не должен писать название руками |
| 2 | Шины и диски как отдельный тип с номером комплекта | catalog + inventory | 3–4 нед | 221 позиция у одного клиента. Оценка поднята после обхода 30.07: это 43 поля и два подтипа (Шина/Диск) в одной таблице, см. §1 |
| 2 | Услуги как позиция сделки без склада | sales | 3 дня | Доставка и упаковка попадают в чек |
| 2 | Лицевой счёт клиента (на счету / в резерве, пополнение и списание) | sales | 1–2 нед | Понятия нет в схеме |
| 2 | Платежи как самостоятельный модуль: приход/расход, справочник источников с типом, движения по счёту | sales | 2 нед | Сейчас `payment` — придаток сделки |
| 2 | Возврат как документ со статусом и складом возврата | sales | 1 нед | |
| 2 | Перенос позиций между сделками | sales | 3 дня | Видно в истории реальной сделки, значит используется |
| 2 | Повествовательная история документа | platform | 1 нед | `audit_log` по полям не даёт такую ленту |
| 2 | Настраиваемые таблицы (активные/неактивные колонки, закрепление, «мои склады») + экспорт | frontend | 2 нед | Сквозной паттерн во всех разделах |
| 2 | Габариты и вес товара отдельно от упаковки, аудит цены | inventory | 3 дня | Нужно для доставки и разбора «кто уронил цену» |
| 2 | Адресное хранение с маркировкой полок | inventory | 1 нед | `storage_cell` есть, печати маркировки нет |
| 1 | **Локальный агент под Windows**: печать ZPL на этикеточный принтер, сканер по COM, фискальный регистратор, синхронизация папки фото | новый компонент | 4–6 нед | Из браузера недостижимо. Отдельный подпроект, в стеке и плане отсутствует |
| 2 | Рабочее место: привязка сканера, принтера этикеток, кассы и склада по умолчанию к компьютеру | frontend | 2 нед | Настройка привязана к машине, а не к пользователю |
| 2 | Реквизиты юрлица на каждый склад, настройки печати чека и накладной, пометка об НДС, поля подписей | sales | 1–2 нед | У одного арендатора несколько ИП на разных складах |
| 2 | Фотопоток по устройствам сотрудников + массовая привязка к товарам | inventory | 2 нед | Фотограф и приёмщик — разные люди и разные моменты |
| 2 | Резерв не снимается сам: колонка «Истёк срок» и решение менеджера | sales | 3 дня | Расходится с §3.7 нашей доменной модели, см. §2 |
| 2 | Статус на уровне позиции сделки, а не только документа | sales | 3 дня | |
| 3 | Уведомления клиентам по email и SMS, менеджерам — почта и браузер | platform | 1–2 нед | В схеме нет ничего |
| 3 | Фото и видео к сделке | sales | 3 дня | |
| 3 | Видео к товару | inventory | 1 нед | Требует мобильного приложения |
| 3 | Архив товаров | inventory | 3 дня | |
| 3 | Массовое добавление фото к товарам | inventory | 1 нед | |
| 3 | Одна активная сессия на пользователя с перехватом | platform | 2 дня | Мелочь, но заметная: Bazon это делает явно |
| 3 | Поля миграции («старые данные», «ст. баркод») | inventory | 3 дня | Понадобится при переходе клиентов |
| — | Проценка по рынку | — | — | Не планировать: работает на агрегате по всем клиентам, следствие масштаба |

### Что у нас есть, а у них не обнаружено

- Затраты по донору и разнесение их на запчасти (`donor_cost`,
  `cost_allocation_method`)
- Снимок себестоимости в момент продажи (`deal_item.cost_price_snapshot`)
- Учёт продаж по менеджерам как заявленный дифференциатор — поле
  «Ответственный» у них есть, но отчёта по нему в интерфейсе не видно
  (раздела отчётов нет вовсе)

Это стоит проверить под администраторской учёткой, прежде чем строить на этом
позиционирование.

---

## 10. Что показала выгрузка склада

Выгружено через «Скачать таблицу» → CSV: 35 841 позиция, 30 колонок, 24,8 МБ,
windows-1251, разделитель `;`. Файл — `~/Downloads/catalog_29-07-26_22-29.csv`.

**Это и есть ответ на главный вопрос «недели 0»: базы выгружаются.**
Товары и доноры отдаются полностью, включая **ссылки на фотографии** —
колонка «Превью» содержит список URL через запятую вида
`http://export-content.baz-on.ru/pub/c2226/productphoto/0005/94/0005_94_118.jpg`
(`c2226` — идентификатор компании).

### Фотографии: проверено, переносимы

| Что проверялось | Результат |
|---|---|
| Доступ без авторизации | **есть.** Выборка из 24 случайных ссылок — все `200 image/jpeg`, без cookies и сессии |
| Заголовки | `Cache-Control: max-age=315360000`, `Expires: 2037`, `ETag`, `Accept-Ranges: bytes` — обычная статика с вечным кэшем и докачкой по диапазонам |
| Объём | 192 755 ссылок у товаров (188 929 уникальных) + 3 935 у доноров ≈ **193 тысячи файлов, 25 ГБ** |
| Средний размер | 138 КБ |
| Фото на позицию | 5,4 |
| Время переноса | 3,4 файла/с последовательно ≈ 15,6 ч; в 8 потоков ≈ **2 часа** |

Дубли: 3 826 ссылок повторяются — одно фото привязано к нескольким товарам.
Складывать по URL, а не по товару, иначе перекачаете лишнее.

Сроков жизни ссылок никто не гарантирует, а `Expires` в 2037 году — это про
кэш браузера, а не про обязательство. Качать нужно, пока подписка клиента
активна: после отключения доступ к CDN, скорее всего, пропадёт.

### Что выгрузка исправила в модели

| Было у меня | Как на самом деле |
|---|---|
| `installation_place text` — «место установки» | **`installation_price numeric`** — цена установки силами разборки. В выгрузке это число: 0 у 35 637 позиций, 240 у 203, 100 у одной |
| `part_stock.qty` — одно число | **Три числа на склад: свободно / резерв / ожидается.** Продавец по телефону различает «есть», «обещано другому» и «приедет контейнером» — это три разных ответа |
| `section` — свободная группировка | Строгий пятиуровневый адрес `01-02-02-03-01`, заполнен у 93,9% |

### Заполняемость: где реально живут данные

| Поле | % | Вывод |
|---|---|---|
| Номер, цена, установка, остатки | 100 | |
| Превью (фото), производитель | 99,9 | фото есть почти у всех |
| Марка, модель, кузов | 99,6–99,7 | |
| Номер производителя | 98,4 | **на этом держится применимость на Дроме** |
| Секция (адрес хранения) | 93,9 | адресное хранение работает |
| Комментарий | 93,1 | |
| Поколение донора | 89,7 | |
| Год выпуска | 78,9 | |
| Номер донора | 73,7 | **четверть склада — контракт без донора** |
| Передний/Задний | 41,2 | |
| Рестайлинг | 40,0 | |
| Левый/Правый | 30,6 | |
| Кросс-номера | 29,2 | в среднем 3,9 номера, максимум 120 |
| Заметка | 20,0 | |
| Цвет | 8,5 | |
| Маркировка | 8,3 | |
| **Оценка состояния** | **0,02** | **8 позиций из 35 841** |

### Два вывода, меняющих приоритеты

**Оценка состояния мертва.** Фича есть, она выведена в карточку, её значения
уходят на площадки — и она проставлена у восьми позиций из тридцати пяти тысяч.
Копировать её надо (это три дня), но вкладываться в неё, делать обязательной
или строить на ней логику — нет. Хороший пример того, почему карту функционала
нельзя строить по интерфейсу: в UI эта кнопка выглядит важной.

**Многоскладовость тоже почти не используется.** Второй склад «54 YARD» пуст
целиком, «ожидается» — ноль везде. То есть разрез остатка по складам в модели
нужен (он в выгрузке и в UI), но сценарии перемещения между площадками у этого
клиента не работают. Не строить вокруг них первый релиз.

**Чего в выгрузке нет:** оси «верх/низ». У Bazon две оси, у формата Дрома три
(`lr`/`fr`/`ud`). Наша схема с тремя — надмножество, это правильно.

### Ловушки разбора, которые нашлись только на полном файле

Все они прошли мимо тестов, написанных по «типичным» строкам, и всплыли при
прогоне разборщика по всем 35 841 + 440 записям. Каждая закрыта тестом в
`BazonValueParserTest`.

| Значение | Чем опасно |
|---|---|
| `124.000` в пробеге | точка — разделитель разрядов; наивный разбор даёт 124 км вместо 124 000 |
| `100131км`, `52 000 км.` | единица измерения дописана в значение |
| `2006-2010`, `10.09-09.15` | «год выпуска» у товара — это период применимости, а не год |
| `2005-` | открытый период «с 2005 и далее» |
| `2005~` | тильда — пометка «примерно» |
| `BMW22` | номер донора не обязан быть числом: машины, купленные не контейнером |
| `396` без скобок | скобочная часть номера донора необязательна |
| `Автозапчасти BMW` | поставка без номера и даты; выбросить нельзя — на неё ссылаются товары |

Итог прогона: доноры разбираются на 100% по всем полям, товары — всё, кроме
**2 записей из 28 283** в поле года, где в исходных данных стоит `200`.
Это опечатка у клиента, и разборщик оставляет её пустой намеренно:
угадывать за клиента год выпуска — верный способ тихо испортить применимость.

---

## 11. Импорт: что работает и чего в выгрузке не хватает

Модуль `ru.partsflow.migration.bazon`. Прогон на полной выгрузке YARD
в схему `t_000042`:

```
поставок          18
доноров          440
наименований    1265
складов            2
товаров        35841
проблем нет
время: 99 с
```

Проверка после загрузки: остаток `38 081 = 37 504 свободно + 577 резерв` —
сходится с суммами по выгрузке до единицы, расхождений в `v_stock_discrepancy`
ноль. Сгенерированный заголовок совпал с исходным до символа:
`Блок подрулевых переключателей Toyota Wish ZNE10 1ZZFE 2006 (б/у) 8414012530`.

### Решения, которые пришлось принять

**Остаток кладётся движением, а не записью в `part_stock`.** Иначе журнал
разойдётся с остатком с первого дня переноса, и ежесуточная сверка будет
ругаться на весь склад. Резерв — наоборот, полем: это обещание, а не движение.

**Все товары едут в категорию «Не разобрано».** Выгрузка категорию не отдаёт,
там только наименование детали. Разложить 35 тысяч позиций по дереву
автоматически — значит разложить неверно; пустая категория честнее выдуманной.

**Наименования заводятся как `UNMATCHED`.** Сопоставление с эталонным каталогом
делается отдельным шагом. Ошибочное сопоставление хуже отсутствующего: оно
выглядит готовым и не попадает в список на разбор.

**Марка, модель, кузов и двигатель донора складываются в заметку** — до
наполнения общего каталога привязать их не к чему, а выбросить значит потерять
то, по чему продавец ищет деталь.

### Чего в выгрузке товаров нет — и что с этим делать

**Колонки «Поставка» в выгрузке товаров нет**, она лежит в «Неактивных».
Импорт наследует поставку через донора — деталь приехала тем же контейнером,
что и машина. Это закрывает 26 424 позиции из 35 841.

Оставшиеся **9 417 контрактных запчастей без донора остаются без поставки**,
и восстановить её из этого файла нечем.

> Перед боевой выгрузкой включите в настройках таблицы товаров колонки
> «Поставка», «Наименование», «Состояние», «Комплектация», «Габариты», «Вес»
> и **«Выгружать»** — они существуют, но в выгрузку по умолчанию не попадают.

**Про «Выгружать» отдельно: без неё площадки получат пустой прайс.** Колонка
несёт разрешение публиковать позицию, у нас это `part.is_published`, и
по умолчанию поле `false`. Если колонки в файле нет, все перенесённые позиции
окажутся непубликуемыми, и первый собранный прайс Дрома или фид Авито выйдет
пустым при полностью рабочем коде — ошибка, на поиск которой уходит день.

Включать публикацию всем подряд импортёр не станет: это значит выложить склад
клиента на площадку по своей инициативе. Вместо этого при отсутствии колонки
`BazonImporter` пишет предупреждение в отчёт об импорте. Формат значений
колонки на живых данных не подтверждён — она была неактивна в обоих обходах,
поэтому `parsePublishFlag` принимает все обычные написания.

**Справочник наименований выгружается отдельно, и это не мелочь.** Разбор
выгрузки товаров дал 1 265 наименований (§10), а в кабинете их **2 259**
(§7). Разница — наименования, под которыми сейчас нет ни одного товара:
архивные, сезонные, заведённые впрок. Вместе с ними теряются и сопоставления
с эталоном, то есть ровно та работа, которую клиент делал руками годами.

У самого справочника есть своя кнопка «Скачать таблицу» — при переносе клиента
её надо использовать, а не восстанавливать справочник из названий товаров.
Иначе клиент получит систему, где треть его наименований снова
«не распознано».

---

## 12. Состояние нашего проекта

Первая редакция таблицы снималась на момент обхода, 30 июля 2026. Ниже —
состояние на **9 августа 2026**: за эти десять дней закрылась почти вся
сводка пробелов, поэтому старая таблица не правится по клеткам, а заменена.

| Область | Схема БД | Код | Фронтенд |
|---|---|---|---|
| Мультиарендность, провижининг | есть | есть | — |
| Outbox, события, разбор недоставленного | есть | есть | есть |
| Аутентификация, роли | есть | есть | есть |
| Каталог: авто, виды деталей, написания | есть, наполнен | есть | есть |
| Поставки и контейнеры | есть | есть | есть |
| Доноры, приёмка, затраты по машине | есть | есть | есть |
| Запчасти, склад, движения, документы | есть | есть | есть |
| Остаток по складам, перемещения, списание | есть | есть | есть |
| Справочник наименований, разбор нераспознанных | есть | есть | есть |
| Автогенерация наименования, градации состояния | есть | есть | есть |
| Шины, диски и колёса в сборе | есть | есть | есть |
| Услуги в сделке | есть | есть | есть |
| Инвентаризация со сверкой по журналу | есть | есть | есть |
| Продажи, оплаты, возвраты, перенос позиций | есть | есть | есть |
| Лицевой счёт клиента, платежи, источники | есть | есть | есть |
| Заказы с площадок | есть | есть | есть |
| Фото в S3, досъёмка из карточки | есть | есть | есть |
| Выгрузка на Дром: прайс, дельты, колёса, свой отбор | есть | есть | есть |
| Выгрузка на Авито | есть | генератор | **нет** — отложено решением |
| Отчёты: менеджеры, источники, окупаемость машин | есть | есть | есть |
| Этикетки со штрихкодами | есть | есть | есть |
| Переезд с прежней системы, импорт из таблицы | есть | есть | есть |
| Бэкапы с проверкой восстановлением, тревоги | — | есть | — |
| Интернет-магазин | нет | нет | нет |
| Печатные формы, реквизиты по складам | нет | нет | нет |
| Уведомления (email, SMS) | нет | нет | нет |
| Фискализация 54-ФЗ | ссылка на чек | нет | нет |
| **Агент под Windows** (принтер, сканер, касса, фото) | — | **нет, и не планируется** — печатает браузер | — |
