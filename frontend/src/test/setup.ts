/**
 * Подготовка среды тестов.
 *
 * <p>IndexedDB в jsdom нет, а на ней стоят справочники и будущая очередь
 * отправки. Подмена даёт настоящее поведение хранилища — транзакции, курсоры,
 * версии схемы, — а не заглушку с Map: половина ошибок в работе с IndexedDB
 * это как раз транзакции, и заглушка их не покажет.
 */
import 'fake-indexeddb/auto';
