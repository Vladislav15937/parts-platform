# parts-platform

Учётная система для авторазборок и продавцов б/у автозапчастей. Мультиарендная SaaS.

Этот файл — контекст для Claude Code. Читай его перед изменениями.

---

## Что это за продукт

Клиенты — авторазборки. Они покупают битые машины (доноры), снимают с них запчасти,
заносят в систему, выгружают объявления на Авито и Дром, продают со склада.

Первые 10 клиентов — компании из одного города, переходящие с конкурирующей системы.
Склады от 3 000 до 50 000 позиций.

Функциональный ориентир — система Bazon (probazon.ru). Паритет по возможностям
и по логике: клиенты приходят оттуда и привыкли к её порядку шагов, умолчаниям
и словам, поэтому выбранный сценарий повторяется близко к оригиналу — «то же
самое, но в три экрана вместо одного» переходящий клиент читает как «стало
хуже». Реализация при этом своя: их кода у нас нет и не будет.

Что копировать не надо: проценку по рынку (работает на агрегате по всем их
клиентам, в одиночку не воспроизводится) и их картинки со знаками — это марка,
а не функциональность.

Дифференциаторы: простота входа, прозрачная цена, учёт продаж по менеджерам,
в перспективе распознавание запчасти по фото.

Разведкой занимается агент `researcher`: смотрит их кабинет и нашу систему
рядом, сценариями, и отдаёт задачи — не инвентаризацию чужих полей.

Про Bazon — три связанных документа (их систему видно только снаружи, всё про
её устройство — вывод, а не факт): `docs/bazon-parity.md` — карта функционала,
что у них есть; `docs/bazon-data-model.md` — реконструкция модели данных,
как устроено; `docs/bazon-vs-us.md` — трезвое сравнение архитектур, где кто
сильнее.

---

## Архитектура: решения, которые нельзя менять походя

Полное обоснование — в `docs/architecture-and-plan.md`. Кратко:

**Модульный монолит**, не микросервисы. Один разработчик. Модули (`platform`,
`catalog`, `intake`, `inventory`, `sales`, `publishing`) общаются через интерфейсы
и доменные события, не через чужие репозитории.

**Схема PostgreSQL на арендатора** (`t_000042`), не общая таблица с `tenant_id`.
Причина — независимый бэкап и восстановление одного клиента, плюс изоляция
не зависит от дисциплины в `WHERE`.

**Outbox → Kafka.** Событие пишется в таблицу `outbox` в той же транзакции, что
и данные. Прямая публикация в Kafka из бизнес-кода запрещена: транзакция БД и
отправка в брокер не атомарны, событие потеряется. Транспорт скрыт за
`EventTransport`, чтобы Kafka можно было заменить.

**Ячейки (cells).** Группа арендаторов (500 выдержаны пробой, планируем
200–300) на общем комплекте
Postgres + Kafka + S3. Отдельный кластер Kafka на арендатора не делаем: при 500
клиентах это 12–22 млн ₽/мес против 150–450 ₽ на арендатора в ячейке.

**Логика живёт в Java, база хранит данные и связи между ними.** Новых триггеров
с бизнес-логикой не заводить. Триггер невидим с того места, откуда его
последствия наблюдают: читаешь сервис — видишь одно, смотришь в базу —
поведение другое, и разница объясняется файлом, который никто не открывал.
Отладчик через него не проходит, тест на сервис его не покрывает, а править
его можно только новым changeset'ом. Цена ошибки тут не в лишнем запросе,
а в часах на вопрос «почему статус поменялся сам».

Что базе оставляем: типы, ключи, `NOT NULL`, `CHECK`, внешние ссылки,
уникальные индексы, значения по умолчанию. Это описание формы данных, оно
декларативно и видно в схеме. Что убираем: вычисление полей из других полей,
изменение соседних таблиц, ведение статусов, запреты вида «эту строку нельзя
менять».

**В схеме арендатора не осталось ни одного триггера и ни одной функции.**
Было двадцать три триггера и десять функций; что с чем сталось и чем
за это заплачено — в `docs/triggers-to-java.md`. Остаток и статус ведёт
`StockLedger`, резерв — `StockReservationRepository`, отметку об изменении
позиции — `PartChangeLog`, `updated_at` — `@PreUpdate` у сущностей, журнал
изменений — `AuditLogListener` (слушатель Hibernate), неизменяемость
журналов — суженные репозитории и `@Immutable`.

Генерируемых колонок тоже не осталось. Свободный остаток считается
выражением `qty - qty_reserved` там, где спрашивают; вектор полнотекстового
поиска — выражением в запросе с GIN-индексом на нём (собрать его вне
Postgres нельзя: русская морфология живёт в базе, но индекс на выражении —
не колонка, и план запроса тот же); приведение номера производителя —
`catalog.OemNumbers.normalize`. Вектор поиска по справочнику видов деталей
убран вовсе: его не читал никто.

В базе остались значения по умолчанию, ограничения, индексы и сверочные
вьюхи. В общей схеме `catalog` осталась `normalize_oem` — не как логика,
а как след истории: changeset `tenant/004` неизменяем и при заведении
нового арендатора проигрывается вместе с генерируемой колонкой, которую
тут же снимает `tenant/051`. Без функции падает провижининг.

**Записи ниже про триггеры читать как историю.** Они объясняют, почему
инвариант устроен так, а не иначе, и это по-прежнему верно; неверно только
«его держит триггер» — держит код, названный выше.

**Заплачено тремя дырами, и все три упираются в одно: приложение ходит
в базу суперпользователем.** Правку журналов прямым SQL не отбивает ничто.
Журнал изменений видит только то, что прошло через Hibernate, — а «кто
уронил цену» спрашивают ровно тогда, когда подозревают правку мимо
интерфейса. Движение, записанное мимо `StockLedger`, оставит остаток
и журнал разошедшимися. Первые две закрываются ролью без `SUPERUSER`
(тогда заработает `REVOKE`, который сейчас суперпользователя не
ограничивает — проверено опытом), третья — дисциплиной и сверкой.

**Роль без `SUPERUSER` включена 5 августа 2026.** Приложение работает
ролью `partsflow_app`, схемы создаёт и мигрирует владелец (`app`), и журналы
рабочей роли не принадлежат: `stock_movement`, `audit_log`
и `customer_account_entry` отданы ей только на `SELECT` и `INSERT`. Проверено
попыткой: `UPDATE` и `DELETE` по каждому из трёх отвечают «permission denied»,
а обычные таблицы правятся как раньше. Реестр арендаторов рабочая роль
только читает — ведёт его владелец.

**Перезапуск ничего не ломает, но может тихо снять защиту.** Роль и права
живут в базе и переживают и рестарт, и повторный накат, и заведение нового
арендатора — проверено всеми четырьмя способами. А вот запуск без переменных
окружения поднимает приложение владельцем схем, и журналы снова становятся
изменяемыми: работает всё одинаково, разница видна только в тот день, когда
журнал предъявляют как доказательство. Поэтому `JournalProtectionCheck`
при старте спрашивает у базы прямо — «могу ли я переписать движение склада» —
и отвечает `WARN`, если может. Запуск при этом не валится: в разработке одна
роль законна, и падение на старте означало бы, что локально ничего не поднять.

**Права выдаются и схемам, которым нечего накатывать.** Разделение ролей
включают на работающей ячейке, где все схемы уже на актуальной версии:
накат их пропускает, и без этого рабочая роль осталась бы без доступа
к складу. Поймано включением на живой базе, а не тестом.

**Реестр пишет владелец, а не рабочая роль.** Отметку `schema_version`
ставит накат, и пока он шёл рабочей ролью, все три арендатора объявлялись
отставшими: прав на `UPDATE public.tenant_registry` у неё нет и быть
не должно. По той же причине провижининг пишет запись реестра владельцем.

**Ниже — история решения, объясняющая, почему роль появилась только сейчас.**

**Роль отложена осознанно, и это не «руки не дошли».** Мимо приложения
может пройти только тот, у кого есть доступ к базе: произвольного SQL
через REST нет, порт Postgres наружу не опубликован. Пока разработчик
один, это защита от себя. Возвращаться — по событию: второй человек
с доступом к боевой базе, первый чужой клиент на ячейке или
разбирательство о недостаче, где журнал предъявляют как доказательство.
Не предлагать эту задачу, пока ни одно не случилось.
Сверки `v_stock_discrepancy`, `v_reservation_discrepancy`
и `v_account_discrepancy` теперь несут больше, чем раньше: пока писали
триггеры, разъехаться было нельзя.

Две вещи при переносе обязаны сохраниться, иначе он будет хуже исходного.
Первая: `reserve_stock` проверяет и меняет остаток **одной** инструкцией,
условие «хватает свободного» стоит в `WHERE`. Наивный перенос даёт
read-modify-write, между чтением и записью встаёт второй продавец, и деталь
уходит двум клиентам — тому самому клиенту, который приедет за ней завтра.
В Java это `UPDATE ... WHERE qty - qty_reserved >= ?` с проверкой числа
затронутых строк либо блокировка строки, но не «прочитали, вычли, записали».
Так и сделано — в `StockReservationRepository` и `StockLedger`.
Вторая: `audit_trigger` — единственное, чего не обходит прямой SQL. Перенеся
его в код, мы теряем журнал для правок мимо приложения; значит либо
правок мимо приложения быть не должно, либо журнал остаётся в базе осознанно.

---

## Памятки модулей

Записи о конкретном модуле лежат рядом с его кодом. Правишь модуль — прочитай
его памятку целиком: там причины, замеры и цена ошибки, а не список полей.

| Памятка | О чём |
|---|---|
| `db/CLAUDE.md` | Liquibase: неизменяемость changeset'ов, откаты, мост, вьюхи |
| `ops/CLAUDE.md` | Ячейка: compose, терминатор, роли, накат схем, бэкапы, тревоги, секреты |
| `frontend/CLAUDE.md` | PWA приёмки, экран продавца, экраны владельца, оформление и плотность |
| `src/main/java/ru/partsflow/platform/CLAUDE.md` | Арендаторы, провижининг, вход, роли, коды ответа, outbox |
| `src/main/java/ru/partsflow/catalog/CLAUDE.md` | Виды деталей и написания, справочник машин, русские написания |
| `src/main/java/ru/partsflow/intake/CLAUDE.md` | Доноры, поставки, затраты по машине, приёмка |
| `src/main/java/ru/partsflow/inventory/CLAUDE.md` | Остаток, движения, резерв, документы, витрина, колёса, фото |
| `src/main/java/ru/partsflow/sales/CLAUDE.md` | Сделки, оплаты, лицевой счёт, возвраты, заказы с площадок |
| `src/main/java/ru/partsflow/publishing/CLAUDE.md` | Прайс Дрома, дельты, отбор выгрузки, колёсный прайс |
| `src/main/java/ru/partsflow/reports/CLAUDE.md` | Отчёты владельца |
| `src/main/java/ru/partsflow/migration/CLAUDE.md` | Переезд с предыдущей системы, импорт из таблицы |

Claude Code подхватывает вложенный `CLAUDE.md` сам, когда работает с файлами
этого каталога. Всё остальное — открывать руками, и это не формальность:
почти каждая запись там стоила живого прогона, а не рассуждения.

**Почему они не здесь.** До 3 сентября 2026 всё лежало одним файлом
на 424 КБ. Его читал каждый — включая того, кто правит одну строку в прайсе, —
и это сто с лишним тысяч токенов до первого действия, вытесняющих контекст
самой задачи. Разрез механический: ни одна запись не переписана и не выброшена,
обратная склейка сверена строка в строку. Здесь остались только те ловушки,
на которые наступают из любого модуля.

---

## Как вести работу

| Файл | О чём |
|---|---|
| `docs/agent-workflow.md` | Как запускать и что параллелится, цикл задачи, доказательства, правило слияния, ловушки |
| `docs/defect-classes.md` | Классы ошибок: как выглядят, чем искать, чем доказать — по ним и идёт разбор |
| `docs/java-rules.md` | Как здесь пишут на Java: паттерны под давлением, транзакции, одновременность, нагрузка |
| `docs/frontend-rules.md` | Как здесь пишут фронтенд: правда экрана о себе, офлайн, телефон первым |
| `docs/sessions.md` | Сессии: где живут, два предела жизни, журнал входов, устройство, отзыв |
| `tools/dev-up.sh` | Поднять всё с нуля до заведённой компании, одной командой |
| `tools/api.sh` | Сессия к API с CSRF: `. tools/api.sh` и дальше `g`, `p`, `pc` |
| `tools/can-start.sh` | Можно ли начинать: зелёная ли `main`. Красная — стоп для всех |
| `tools/board.py` | Доска задач `tasks/`: что свободно, что в работе, не врёт ли она |
| `tools/checks.sh` | Живые сверки: расхождения в данных, две поверхности поиска, счётчики выгрузок, целостность страниц |
| `.claude/agents/` | Одиннадцать агентов: `conductor` держит доску, `researcher` заводит задачи, `coder` и `sweep` их делают |

Первые два — до первой правки. Предметное знание отсюда и из памяток,
порядок работы — оттуда.

---

## Ловушки, на которых этот проект сломается

Здесь — сквозные: они не про склад, продажи или выгрузки, а про то,
как устроен доступ к базе, что обязано доезжать до человека и чего не видят
тесты. Ловушки конкретного модуля — в его памятке.

### Арендатор, схема и транзакции

**`search_path` и PgBouncer.** PgBouncer в transaction mode отдаёт физическое
соединение другой транзакции после коммита. Сессионный `SET search_path` протечёт
к другому арендатору — это ровно та утечка данных, ради предотвращения которой
всё затевалось. Поэтому `TenantConnectionProvider` сбрасывает `search_path`
при возврате соединения в пул. Тест `TenantIsolationTest` это стережёт —
не отключай его.

**`TenantContext` без транзакции не работает — это ловушка, на которую
наступали трижды.** `search_path` выставляет провайдер соединений Hibernate
при выдаче соединения транзакции; `JdbcTemplate`, вызванный снаружи, берёт
соединение из пула напрямую и уходит в `public` — «relation … does not exist».
Установленного `TenantContext` для этого мало. Правило простое: любой доступ
к схеме арендатора идёт из `@Transactional`-метода или через
`TransactionTemplate`. Уже споткнулись отчёты, доводка справочника после
переезда и сбор метрик.

**Новая перегрузка метода не наследует `@Transactional`.** Витрина после
восстановления отвечала пятисоткой при целых данных: `search_path` выставляет
провайдер соединений Hibernate внутри транзакции, а перегрузка `list(...)`
с курсором осталась без аннотации — и `JdbcTemplate` ушёл в `public`.
Поиск продавца при этом работал: он идёт через репозиторий. Пятый раз
на эту ловушку, и впервые её поймала репетиция восстановления, а не тест.

### Что обязано доезжать до человека

**Эндпоинт без экрана — это отсутствующая возможность.** `MemberController`
и `OrganizationController` были написаны и покрыты тестами, но фронтенд
не вызывал их ни одной строкой: завести сотрудника, филиал, склад или ячейку
владелец не мог вовсе — только запросом к API, то есть с разработчиком.
Нашлось не чтением кода, а попыткой пройти сценарий проверки: на первом
же шаге. Экраны «Сотрудники» и «Склады» это закрыли.

**Сервис без эндпоинта — тем более.** `PartService.changePrice` был написан
с самого начала, публиковал событие для площадок и даже фиксировал автора
правки — а звать его было некому: ни эндпоинта, ни экрана. То есть цену
принятой детали владелец изменить не мог вовсе, при том что на разборке её
двигают ежедневно. Закрыто `PUT /api/parts/{id}` и формой правки в карточке.

**Держит это перебор, а не внимательность.** `tools/endpoint-coverage.py`
сверяет все пути контроллеров с теми, до которых дотягивается экран, и валит
CI на незнакомом несовпадении: новый эндпоинт нельзя завести молча — либо
у него есть экран, либо в списке написано, почему его нет. Восемь случаев
до этого нашлись только тем, что кто-то пошёл проходить сценарий.

**«До которых дотягивается экран» — не то же самое, что «упомянутых
во фронтенде», и на этой разнице сторож год пропускал свой же класс.**
Он собирал любой литерал `/api/…` под `frontend/src`, а слой клиента API
(`sales/sales.ts`, `inventory/inventory.ts`) ровно из таких литералов
и состоит — это его работа. Значит эндпоинт, которому написали обёртку,
считался вызванным, даже если обёртку не зовёт ни один экран: проверка
спрашивала «есть ли обёртка», а обещала «есть ли экран». Тесты засчитывались
туда же — заглушка `fetch`, отвечающая по адресу, это описание сервера,
а не кнопка у человека. Теперь литерал привязан к объявлению, в котором
стоит, объявления связаны в граф по импортам и упоминаниям имени, а корни
графа — файлы `.tsx` вне тестов. Мимо прежней редакции молча проехали три
возможности, две из них настоящие пробелы (9 сентября 2026, задача 0055).
Сам сторож с тех пор проверяет себя перед каждым прогоном на трёх случаях
(`--selftest`): проверка, которая не краснеет на дефекте, хуже отсутствующей,
а установить это можно только попыткой.

Список причин при этом не оправдание. Пометка `ПРОБЕЛ` означает возможность,
которая есть в коде и недоступна человеку, и такие записи — очередь работы,
а не разрешение: найденные перебором три (место машины, приход партии
и её состав) закрыты экранами 3 сентября 2026. Сейчас их снова два, и оба
нашлись не обходом, а тем, что сторож научился отличать обёртку от экрана:
просроченные резервы списком (`GET /api/deals/expired-reservations`, ждёт
задачи 0052) и снятие ошибочного сопоставления написания
(`POST /api/part-names/{id}/unmatch` — сопоставить с экрана можно, отменить
нельзя, а после переезда клиента таких сопоставлений сотни). Пометки
«существу» и «устарел» — другое дело: первое про пути, которые фронтенд
и не должен звать (управляющий контур, забор прайса площадкой), второе
про заменённые, у которых зовущих нет вовсе.

**Текстовый блок Java съедает пробелы по краям, и склейка SQL от этого
разваливается.** Пробел в конце строки внутри `"""…"""` срезается, а у блока,
чьи закрывающие кавычки стоят на строке содержимого, срезается весь отступ:
`… AND """ + expr` даёт `ANDCASE`, а `""" + expr + """\n    AS supply"""` —
`ENDAS supply`. Оба раза это отказ Postgres на грамматике, то есть пятисотка
на живом запросе, и оба раза компилятор молчит. Правило: подставляя выражение
в SQL, пишите разделитель явной строкой — `+ " AS supply"`, — а не надейтесь
на отступ. Поймано дважды за один вечер: сначала на истории карточки,
потом на выгрузке колёс, и второй раз — полным прогоном тестов, а не глазами.

**Классы в ответах контроллеров должны быть record'ами.** Обычный класс
с методами в стиле record Jackson не сериализует — свойств у него нет,
и ответ уходит пятисоткой «No acceptable representation». Тесты, зовущие
сервис напрямую, этого не видят: нужен прогон через HTTP.

**Приёмка и запрос ссылки на фото идемпотентны по ключу клиента.** Клиент
генерирует `requestId` при постановке в офлайн-очередь и не меняет при повторах;
уникальность стережёт индекс в БД, а не проверка «нет ли уже такого» в коде —
та пропустит второй одновременный повтор. Повтор возвращает результат первого
раза, а не ошибку: очередь не отличает «получилось только что» от «получилось
в прошлый раз», ей нужен ответ, чтобы удалить запись.

**Индекс отбивал дубль, а наружу летела ошибка — и это половина защиты.**
Обычный повтор проверка ловила, а два повтора в один момент проходили её оба:
между чтением и вставкой второй ещё ничего не видит. Дубля не появлялось,
но ответ был 409 «Операция нарушает целостность данных» — то есть **ошибка
на успешную приёмку**, а офлайн-очередь читает 409 как отказ по существу
и уводит запись в «требует внимания». Приёмщик видит красное там, где деталь
уже на складе, и заводит её второй раз руками — то самое удвоение, ради
которого ключ и существует. Теперь нарушение уникальности перечитывает
результат первого запроса и отдаёт его; читается новой транзакцией, потому
что прежняя помечена на откат. Поймано живым прогоном шестью одновременными
запросами: последовательный повтор работал правильно с самого начала.

**И ровно то же было у ссылки на снимок** — там телефон ждёт адрес, чтобы
залить фотографию, и вместо него получал отказ. Правка одна на обе
поверхности, потому что и болезнь одна: проверка чтением плюс уникальный
индекс дают защиту от дубля, но не дают правильного ответа.

**Третьей оказалась та же дыра у заказа с площадки, и там её видит
продавец.** Идемпотентность по номеру заказа стоит с самого начала, но
проверкой чтением: два одновременных нажатия «Принять заказ» проходят её
оба, и проигравшего отбивает `deal_external_order_uk` — ответом «Операция
нарушает целостность данных». Нажимают второй раз как раз потому, что первое
нажатие не показало результата, а получают в ответ поломку сервера на заказ,
который уже принят и товар по которому уже отложен. Теперь контроллер
перечитывает прежнюю сделку и отдаёт её с `replayed`, как при обычном
повторе. Правило общее: **где стоит уникальный индекс, там нужен и ответ
на одновременный повтор** — сам по себе индекс защищает данные, а не
человека.

**Номер заказа обрезается один раз и на всё.** Писался в сделку обрезанный,
а искался тот, что пришёл: номер с лишним пробелом не находил своей же
сделки и шёл на повторное заведение — то есть идемпотентность отключалась
от невидимого символа.

**Перебор уникальных индексов нашёл ещё три места, и все три видит
человек.** Проверка шла от индекса, а не от операции: тридцать пять
уникальных индексов схемы против того, кто в них пишет. Логин сотрудника
и номер поставки отвечали правильно с самого начала (первый — словами,
вторая — той же поставкой), а вот название выгрузки и штрихкод — нет.

**Второй прайс-лист с тем же названием.** У владельца их на Дром пять
(«новые», «низкая», «средняя»…), названия он придумывает сам и рано или
поздно повторится — а «Операция нарушает целостность данных» не говорит
ни что случилось, ни что делать. Название занято в пределах площадки,
а не вообще: на Авито то же слово свободно.

**Штрихкод, занятый другой позицией.** Владелец вводит его с этикетки,
и по отказу базы не понять, у какой позиции этот код уже стоит, — а найти
надо именно её. Позиция называется публичным кодом, а не номером в базе:
по нему её видно на витрине.

**Снятый штрихкод — это NULL, а не пустая строка, и это худшее из трёх.**
Дубля тут нет вовсе: сняв штрихкод с одной позиции, владелец на второй
получал отказ, потому что пустых строк в уникальном индексе может быть
только одна. NULL же в Postgres друг с другом не сталкиваются. Та же
природа, что у нулевой цены установки: пусто означает «не заполнено»,
а не значение.

### Тесты

**Схему на два теста не делить.** `SellerSearchTest` и `BazonWheelImportTest`
жили в одной `t_000106`, и это ничего не значило, пока первый заводил только
запчасти. Стоило добавить туда шину — и перенос колёс, считающий остаток
по всей линии товара, стал получать шесть вместо пяти: упал тест, к правке
отношения не имевший. Схема стоит дёшево, поиск причины — нет.

**Тесты упираются в `max_connections`, и лечится это пулом, а не пределом.**
Каждый `@SpringBootTest` с иным набором свойств поднимает свой кэшированный
контекст, и Spring держит их одновременно. С боевым пулом в 20 соединений
полутора десятков контекстов хватает, чтобы съесть и 400, — CI покраснел
ровно на этом, добавив два новых набора свойств. Пул урезан до пяти
в `src/test/resources/application.properties`.

Именно там, а не в `@DynamicPropertySource`: тот старше свойств
`@SpringBootTest` и молча отменил бы пул из одного соединения, на котором
держится `TenantIsolationTest`. Файл ресурсов ниже приоритетом — и его
настройка как раз перебивается индивидуальной.

**Маркерные свойства вида `app.что-то-test=true` не заводить.** Они ни на что
не влияют, кроме ключа кэша контекстов: каждый такой маркер — ещё один
поднятый Spring и ещё один пул соединений. Нужен свой контекст — его дают
`@Import` с подменой бина или настоящая настройка.

**Хранилища у большинства тестовых контекстов нет, и это надо помнить.**
MinIO поднимают только `PhotoServiceTest` и `PhotoMigrationTest`, каждый
своим контейнером; остальные смотрят в `localhost:9000`, где локально
оказывается MinIO из compose разработки, а на CI — ничего. Проверка,
обращающаяся к S3 при старте, валит там все контексты сразу, и локально это
не видно. Отсюда `app.s3.ensure-bucket=false` в `src/test/resources`
и включение его в тех двух тестах, где хранилище настоящее. Проверять такие
правки надо с остановленным MinIO — это и есть условия CI. На этом и попались:
`PartChangeLogTest` проверял отметку об удалении снимка, а удаление ходит
в хранилище — локально зелено, на стенде «Connection refused». Проверка
переехала в `PhotoServiceTest`, где MinIO настоящий, а в контексте
без хранилища осталось назначение главного снимка: тот же путь до отметки,
но мимо S3.

**MockMvc этого класса ошибок не видит вовсе.** Он собирает ответ в памяти
и позволяет сменить статус после записи, поэтому «200 с нулём байт»
превращается в нём в честный четырёхсотый — тест зелен и на сломанном коде,
проверено откатом. Нужен настоящий контейнер: `DromFeedStreamTest` — первый
в проекте `@SpringBootTest(webEnvironment = RANDOM_PORT)`, и заведён он
именно поэтому. Слабый MockMvc-тест на то же самое удалён: зелёный тест,
который не падает от поломки, хуже отсутствующего.

**Но настоящий контейнер отдаёт тело раньше, чем обработчик дописал своё.**
Закрытый поток ответа означает, что байты уже у клиента: `get()` в тесте
возвращается, пока поток запроса ещё идёт по остатку метода. Всё, что сервер
делает **после** ответа — отметка о заборе прайса, запись в журнал, — тест,
проверяющий это сразу после запроса, читает раньше, чем сервер написал:
проверка падает на исправном коде, и падает по-разному на разных машинах.
Так покраснела `main` 5 сентября 2026 — три метода `DromFeedStreamTest`
при зелёном прогоне у себя и на PR. Лечится ожиданием состояния
(`awaitMark`), а не сдвигом часов: дождавшись, тест проверяет то же самое
утверждение, а не дождавшись — падает со своим прежним сообщением.
Проверено с двух сторон: задержка в 50 мс перед записью воспроизводит отказ
дословно, а снятая запись валит все три метода и после починки.

**Правку состояния после размонтирования не видно ничем, кроме снесённой
среды.** Порода та же, что у `DromFeedStreamTest` выше — код дописывает своё
после того, как тест кончился, — только здесь молчит ещё и React: предупреждение
«Can't perform a React state update on an unmounted component» убрали в 18.0.
В браузере ошибка не проявляется никак, а в прогоне вылезает
`ReferenceError: window is not defined` — из **чужого** файла и с чужим стеком:
колбэк дошёл до `setState`, когда vitest уже убрал jsdom и поднял среду
следующего теста. Падает не проверка, а среда, и падает не там, где ошибка.

**Полный прогон этого класса не доказывает вовсе, и на этом обожглись
дважды.** Хотфикс #56 предъявил доказательством «346 тестов чисто» — разбор
прогнал тот же набор восемь раз на сломанном коде, все восемь зелёные. Причина
простая: обычный тест размонтирует экран в `afterEach`, когда среда ещё цела,
а колбэки уже отработали, — то есть гонки в нём не бывает. Направленный тест
устраивает её руками: `cleanup()` **синхронно** сразу после нажатия, затем
`delete globalThis.window` (так vitest и убирает среду между файлами)
и ожидание. Незакрытый колбэк даёт тот самый `ReferenceError`; ловить его надо
через `process.on('unhandledRejection')` — обработчик зовёт `void start(...)`,
и отклонённое обещание больше нигде не всплывает. Образец —
`inventoryMountedGuard.test.tsx`.

**А сторож от этой гонки, не возвращаемый в `true`, в разработке хуже самой
гонки.** `main.tsx` оборачивает приложение в `StrictMode`, и тот в dev-сборке
прогоняет эффекты дважды: setup → cleanup → setup. Сторож, написанный как
`useEffect(() => () => { mounted.current = false; }, [])`, после этого
остаётся `false` **на всю жизнь компонента**, и все прикрытые им колбэки
замолкают разом — а замолкают они тихо, потому что каждый из них выглядит
как «проверили и не стали делать». Экран пересчёта от этого умер целиком:
счётчик навсегда стоял на «Загружаем…», уже скачанный лист обхода не
показывался вовсе (его ставит тот же колбэк), обе кнопки открытия гасли
навсегда, а отказ сервера не доезжал ни в каком виде — при полностью исправном
сервере. Сутки в `main` и невидимо для CI: `StrictMode` включён только
в разработке, то есть ровно там, где проверяют руками.

Тело эффекта обязано **начинаться** с `mounted.current = true`. Правильная
запись одна на проект — `ui/useMounted.ts`, и заведена она хуком именно
поэтому: руками этот сторож написали один раз и один раз неправильно.

**Mockito на этой JDK не работает** — ByteBuddy не может инструментировать
классы. Границы вроде HTTP-клиента подменяй рукописной заглушкой через
`@TestConfiguration` и `@Primary`, как в `DromDeltaSenderTest`.

---

## Структура

```
src/main/java/ru/partsflow/
├── PartsPlatformApplication.java
├── platform/
│   ├── tenant/     — контекст арендатора, маршрутизация соединений
│   ├── outbox/     — DomainEventPublisher, EventTransport, релей
│   └── config/
├── catalog/        — справочники (общая схема catalog)
├── intake/         — доноры, приёмка
├── inventory/      — запчасти, склад, движения
├── sales/          — сделки, оплаты, возвраты
├── reports/        — отчёты владельца: продажи по менеджерам, окупаемость машин
├── publishing/     — выгрузки: Авито (pull-фид), Дром (pull-фид + API-дельты)
└── shared/
db/                 — миграции Liquibase (см. db/README.md)
docs/               — архитектура, доменная модель
frontend/           — PWA приёмки: React + TS + Vite (см. frontend/README.md)
```

---

## Как запустить

```bash
docker compose up -d              # postgres, kafka, minio
./db/verify.sh                    # проверка миграций (опционально)
./mvnw spring-boot:run            # приложение на :8080
cd frontend && npm install && npm run dev   # PWA на :5173, прокси на :8080
```

Подъём с нуля, включая заведение компании для прогона, расписан
в `docs/walkthrough.md`, раздел 0: открывать надо `localhost:5173`
(Vite проксирует `/api`), а компания заводится провижинингом — встроенных
в базу компаний нет и не будет.

Тесты: `./mvnw test` и `cd frontend && npm test`. Обёртка `mvnw` в репозитории
намеренно — `mvn` в PATH есть не везде, а версия сборки должна совпадать
с той, что гоняет CI. То же гоняется на каждый push
(`.github/workflows/ci.yml`): миграции, бэкенд, фронтенд — тремя независимыми
задачами, чтобы упавший фронтенд не прятал результат бэкенда.

Профиль по умолчанию `local`: транспорт событий — in-memory, Kafka не нужна.
Профиль `kafka` включает реальный транспорт.

Развёртывание — `docs/deployment.md`: `docker-compose.prod.yml` поднимает
ячейку целиком, PWA собирается внутрь jar и отдаётся тем же приложением.
Одним доменом это не для красоты: сессия в cookie и CSRF-токен из cookie
работают только на одном источнике.

---

## Состояние и что делать дальше

**Клиент заводится одной командой.** `POST /api/provisioning/tenants` создаёт
схему, накатывает миграции и заводит владельца — раньше это были четыре шага
руками, и забыть один ничего не стоило.

**Работает сквозным путём.** Приёмка с телефона целиком: PWA с входом,
предзагрузкой справочников, офлайн-очередью в IndexedDB, фотографиями в S3
мимо приложения, сканером ячеек и VIN, пересчётом склада. Под ней —
аутентификация с арендатором из сессии, идемпотентность по ключу клиента,
приёмка одной точкой входа, справочник наименований, складские документы,
инвентаризация со сверкой по журналу.

**Закрыто по коду и доступно снаружи.** Продажи: сделка с резервом, выдача,
отмена, перенос позиций, оплата с переплатой на лицевой счёт, возврат
документом — плюс REST (`SalesController`, `CustomerAccountController`)
с проверками роли. Событийный контур замкнут: outbox → релей → диспетчер →
обработчики, выдача сделки доезжает до Дрома дельтой.

**Реестр возвратов.** Раньше возврат было не найти иначе, чем через клиента
и его сделку; теперь экран «Возвраты» (владелец, менеджер, продавец) и
`GET /api/deals/returns` показывают все документы обзором — по дате, клиенту,
сделке, складу возврата, сумме, статусу и причине, с поиском по номеру сделки
(точно), клиенту и причине (вхождением) и по периоду. Подвал считает найденное
по отбору отдельно от суммы: сумма отменённого в неё не входит. Оформление
и отмена возврата остаются в сделке — экран только читает.

**Оплата записывается способом, а не безымянно.** Владелец заводит источники
платежей на экране «Настройки» — название и тип («Наличный расчёт»,
«Расчётный счёт», «Интернет-эквайринг», «В долг», «Площадка»), с архивацией
и возвратом из архива; там же — источники сделок (откуда пришла продажа),
которые раньше правились только SQL. Продавец при оплате, возврате денег
из кассы и пополнении/выдаче лицевого счёта выбирает способ из списка,
с умолчанием — тем, которым он платил в прошлый раз. Поле `payment_source_id`
существовало в схеме и в контроллерах с самого начала (задача 0024) —
фронтенд просто не отправлял его никогда.

**Есть, но снаружи недоступно.** Фид Авито генерируется, но забрать его
неоткуда — Авито отложено.

**Выгрузки.** Прайсов на одну площадку может быть несколько, каждый со своим
отбором (цена, состояние, склады) и своей постоянной ссылкой — экран
«Выгрузки», владелец и менеджер. Это то, чем живой клиент раскладывает склад
по прайс-листам с разной ценой размещения.

У каждой видно, **когда площадка последний раз забирала прайс** — «Скачан
05 сен, 20:01» или «Прайс не забирали». До этого забор писался только в лог,
и на вопрос «уехало ли» отвечал разработчик через `docker logs`; спрашивают
его первым при подключении клиента и первым, когда объявления пропали.

**Сколько снимков уходит в объявление, задаёт владелец** — своё число
у каждой выгрузки, ноль означает «без ограничения», незаданное оставляет
прежние десять. До этого предел был зашит в сборку прайса, то есть правка
его означала релиз, а площадки считают снимки по-разному.

**Стоимость установки доезжает до объявления.** У выгрузки есть
переключатель «Дописывать стоимость установки к описанию» и текст
с подстановкой `{цена}` — по умолчанию «Стоимость установки на нашем
автосервисе: {цена} р.». Строка дописывается в конец описания, перед
ссылкой на ролик. Товар, у которого цена установки не заполнена (или
стоит нулём — у нас это то же самое), уезжает без неё: пусто означает
«услуги нет», а не «бесплатно». До этого поле «Цена установки» было
в карточке и в отборе выгрузки, а до покупателя не доходило ни одной
строкой — услуга заведена, стоит денег и невидима там, где её покупают.

**Товар, которого ещё нет на складе, уезжает в прайс с припиской.** У выгрузки
есть переключатель «Выгружать товары, по которым ожидается поступление»
и текст приписки; такой товар — позиция, привязанная к поставке в состоянии
«ожидается» или «в пути», — уходит на площадку доступным, а приписка стоит
**в начале** описания объявления. Пришла поставка, товар принят на склад —
приписка пропадает сама. Контейнеры из Японии клиент заводит заранее и продаёт
по ним ещё до прихода; до этого такой товар не уезжал вовсе, то есть
предзаказы жили в телефонных разговорах. Решение владельца продукта
от 5 сентября 2026, и оно намеренно отличается от резерва: отложенное
покупателю обещано, товар в пути — никому. Без текста приписки не выгружается
ничего: пустой текст при включённом переключателе отбивается словами.
Колёсной выгрузки это не касается — в прайсе шин элемента описания нет вовсе.
**Но завести такой товар сегодня нечем:** приёмка всегда создаёт новую позицию
и всегда кладёт её на склад, а перенос из прежней системы заводит поставки
сразу прибывшими. Настройка работает и проверена живым прогоном, данных для
неё человеку взять неоткуда — экран «завести товар по ожидаемой поставке»
ждёт решения владельца продукта (`publishing/CLAUDE.md`).

**Выгрузку можно переименовать, выключить и удалить.** Прайс-лист закрывают
на сезон или отказываются от него совсем, а выключить его было нечем: ссылка
оставалась живой, и площадка продолжала забирать товар, которого клиент там
видеть не хочет. Выключенная не отдаёт прайс **вовсе** и не получает дельт —
площадка понимает, что выгрузки нет, и объявления доживают до включения
обратно; пустой прайс тут не годится, его она читает как «снять всё».
Переименование ссылки не трогает: её прописывает у себя техспециалист
площадки руками.

**Удаление помечает выгрузку, а не удаляет запись** (решение владельца
продукта от 5 сентября 2026, `tasks/0003-vygruzku-mozhno-vyklyuchit.md`).
Ссылка перестаёт работать, из списка выгрузка пропадает, имя файла
освобождается — а отметки о заборе прайса и об отправках остаются и видны
в разделе «Удалённые выгрузки»: «эта выгрузка вообще работала и когда её
последний раз забирали» спрашивают уже после того, как прайс-лист закрыли.
Название удалённой выгрузки освобождается тоже, но это **решение исполнителя,
ждущее подтверждения владельца продукта**: в задаче про `title` не сказано
ничего, довод и цена вопроса — в ней же и в `publishing/CLAUDE.md`.

Цена в прайсе может отличаться от складской: **наценка или скидка процентом
с округлением**, своя у каждой выгрузки. Площадка берёт комиссию, и продавцы
закладывают её в объявление — у живого клиента на Авито стоит −20 %. Цена
на складе, витрине и у продавца при этом не меняется. Применяется и к колёсной
выгрузке.

**Отчёты владельца.** Первым блоком — «Сводно»: сколько товара лежит
на складе и на какую сумму (запчасти и колёса отдельно) и сколько висит
в незакрытых сделках. Шесть чисел без настроек и без периода: остаток
и незакрытые сделки существуют «сейчас». Считает `qty`, а не свободный
остаток, — отложенная под клиента деталь с полки не делась.

**Машину и поставку видно позициями.** Выбрав машину или партию, владелец
открывает четыре вкладки — «Поступило · Продано · Списано · Остатки» —
и видит, какие именно позиции с неё пришли, какие проданы, какие списаны
и какие лежат до сих пор, с итогом в подвале каждой («3 товара (4 шт.):
розничная стоимость — 9 000»). До этого числа по машине были, а спросить
«что именно лежит» было негде: за этим уходили в склад и собирали отбор
руками. По поставке не было и чисел, хотя «окупился ли контейнер»
спрашивают теми же словами, что и про машину. Вкладки — раскрытые
до позиций выражения `v_donor_profitability`, поэтому суммы сходятся
с уже показываемыми числами окупаемости — **кроме частично проданной
позиции**: продав одно колесо из двух, владелец увидит его в выручке
окупаемости, но не на вкладке «Продано». Почему так и что решать
владельцу — в `reports/CLAUDE.md`. Графиков окупаемости во времени
здесь нет — это отдельная задача.

**Проданные позиции видно строками — с ценой продажи, себестоимостью
и выгодой.** Блок «Проданные позиции» в «Отчётах» (владелец и менеджер)
отвечает на «сколько мы заработали на запчастях с этого контейнера»,
«на чём мы теряем» и «кто продаёт в минус» экраном, а не разработчиком
с SQL: дата выдачи, сделка, номер товара, наименование, состояние, цена
продажи, себестоимость, выгода, количество, склад выдачи, ответственный,
поставка и номер донора. Отбор — период, склад, ответственный, машина,
поставка; подвал считается по всему отбору, а не по показанной странице,
и рядом сказано, сколько показано. Есть «Скачать таблицу» тем же отбором.
Себестоимость (`deal_item.cost_price_snapshot`) писалась с самого начала
и читалась только агрегатами — то есть преимущество, записанное
в `docs/bazon-vs-us.md`, с экрана видно не было.

Отбор идёт по строке сделки, а не по статусу карточки, поэтому
**частично проданная позиция здесь видна** — то самое колесо, одно
из двух, которое вкладка «Продано» в разрезе по машине не показывает
(она отбирает `p.status = 'SOLD'`). Числа этого отчёта сходятся
с выручкой окупаемости; неполон список строк вкладки, и решение
о нём по-прежнему за владельцем продукта (`reports/CLAUDE.md`).

**Заодно вкладка «Продано» в разрезе по машине перестала врать.** Она
показывала цену и себестоимость **из карточки**, а подвал считала
по настоящим продажам: фара с прайсом 1 000, проданная за 900, давала
1 000 в строке против 900 в подвале, и сложенная глазами колонка
не сходилась. Теперь в строке цена сделки и снимок себестоимости,
а колонка называется «Цена продажи». Скидка при этом видна зачёркнутой
прежней ценой — но **писать её сегодня нечем**: продавец правит цену
строки, а не заводит скидку (`reports/CLAUDE.md`).

**Видно и чем платили.** Блок «Платежи по источникам» за тот же месяц
отвечает на вопрос «сколько прошло наличными, сколько картой, сколько
осталось в долг»: колонки «Источник · Платежей · Приход · Расход · Итог»
и итог за период в подвале. Задача 0024 научила систему **писать**
`payment_source_id`, а прочитать его было негде — возможность была закрыта
наполовину. Считается сам источник, а не его тип: «Карта Сбер» и «Карта
Т-Банк» — разные строки, хотя тип у них один. Архивный источник из отчёта
не исчезает (платежи по нему были), платёж без записанного способа виден
строкой «источник не указан», а приход и расход стоят разными числами:
возврат из кассы уменьшает остаток способа, но не отменяет принятого.
Сами платежи построчно — в разделе «Платежи» ниже.

Дальше продажи по менеджерам, откуда пришли продажи
и окупаемость машин —
`v_manager_sales` и `v_donor_profitability` доступны через `/api/reports`
и экран «Отчёты», только владельцу и менеджеру: там зарплатная база смены
и себестоимость.

**Этикетки.** Штрихкоды ячеек и деталей печатаются пачкой из браузера
(экран «Этикетки», владелец, менеджер или кладовщик). Адреса, которые
Code128 не кодирует, помечаются и не выбираются.

**И этикетку одной позиции печатают из её карточки** — «Печать штрих-кода»
рядом с «Изменить», «Перевезти» и «Списать», роли те же, что у экрана
«Этикетки». Нажатие показывает наклейку этой позиции и открывает диалог
печати; на карточке колеса действие то же самое. Перепечатывают этикетки
поштучно и постоянно — отклеилась, порвалась, залили маслом, — а поштучно
было нельзя вовсе: экран «Этикетки» отправляет в печать **всю** выдачу
поиска, выбрать из неё одну нечем, и «фара» на живом складе находит 745
строк. Точный код в запросе не помогает: он неугадываемый, и на руках его
нет как раз потому, что этикетка отклеилась. Печатали пачку и выбрасывали
лишние наклейки.

**Видно, на какой полке лежит деталь, и её можно переставить на другую.**
Адресов у позиции два, и на экране был виден тот, который никто не ведёт:
колонка «Секция» — это текстовое поле, куда человек пишет свою нумерацию
полок, а `storage_cell`, который наполняют приёмка и сканер, не показывался
нигде, кроме ленты правок — то есть узнать, где деталь лежит **сейчас**,
можно было, только если её когда-то переставляли. Теперь на витрине рядом
с «Секцией» стоит колонка «Ячейка», а в карточке адрес идёт рядом со складом
и остатком, до машины и номеров: «Основной · А-01-1», без заведённой ячейки —
«без адреса».

Оттуда же деталь **переставляется на другую полку того же склада**
(«Переставить на полку», `PUT /api/parts/{id}/cell`; владелец, менеджер
и кладовщик — те же роли, что у перевозки). До этого перекладка была
невозможна вовсе: «Перевезти» требует другого склада-приёмника, а у клиента
с одним складом такого нет, — и ячейка, поставленная при приёмке, оставалась
на карточке навсегда. Вместе с ней переставали работать печать этикеток
и пересчёт по полке, которые на этот адрес и опираются. Позицию, лежащую
на двух складах, переставляют по складу: адрес меняется у названного,
соседний остаётся как был.

Перекладка записывается **движением склада** — `MOVE` с нулевым
количеством, обе полки названы, — то есть на вопрос «деталь лежала
на А-01-1, где она» отвечает тот же журнал, что и на «кто её унёс».
В истории карточки она видна с автором: у позиции на одном складе — лентой
правок («Ячейка: было А-01-1, стало А-02-1»), у позиции на двух — лентой
движений («Основной · А-01-1 → А-02-1»), где склад назван явно. Второе
не оговорка, а следствие: `part.storage_cell_id` — одно поле на весь
товар, и написать в него адрес одного склада значило бы объявить
изменившимся второй, к которому никто не подходил.

**Оформление снято с кабинета клиента, но плотность — только для мыши.**
Палитра, радиусы и шрифт взяты вычисленными стилями оригинала, а не подобраны
на глаз: фон страницы `#eaecf0`, полотно белое, подложка `#f9fafb`, рельс
`#344054`, синий `#2e90fa`, Noto Sans, радиусы 3 и 6. Но в кабинете строка
13 пикселей и кнопка высотой 32 — там сидят за столом, а приёмщик работает
стоя, одной рукой, часто в перчатках. Поэтому размеры переключаются шириной
экрана: до 900 пикселей остаётся всё крупное (18 пикселей, цели 48, разделы
полосой сверху), после — кабинетная плотность и рельс во всю высоту.

**Ширина снимается только у табличных экранов** (`.screen--wide`). Форма
приёмки во всю ширину монитора — это поле ввода длиной в метр и глаз, ищущий
следующее поле где-то справа. И правки оболочки ограничены `.app__content`:
тот же `.screen` носит экран входа, у которого ни рельса, ни полотна нет, —
снятая с него ширина расплющила форму во всю страницу.

**Снимается она до ширины окна, а не до бесконечности.** `max-width: none`
позволял секции растянуться под таблицу в двадцать колонок, и вбок уезжала
**вся страница вместе с рельсом и шапкой**: 1733 пикселя при экране 1600,
а на телефоне 2124 при 386. Прокрутка списка вниз при этом всё время
уводила вбок. Прокручиваться обязана таблица внутри своей обёртки, и одного
`overflow-x: auto` на обёртке для этого мало: без `max-width: 100%` она сама
растягивается под содержимое, и прокручивать становится нечего.

**И обёртки мало, если сам экран растёт под содержимое — на этом потеряли
круг.** `.screen` стоит в flex-колонке с `margin: 0 auto`, а элемент
с автополями **поперёк** оси контейнера по контейнеру не растягивается: его
ширина считается по содержимому и не может стать меньше min-content. Таблица
сотрудников давала min-content 590 при 390 доступных, `.screen` вырастал
до 590 — и `max-width: 100%` у обёртки мерился уже от него. Обе правки нужны
разом; поодиночке они оставляли 590 и 608 при 390, проверено обоими откатами.
`max-width: 40rem` тут не спасает вовсе: 640 больше 390, предел не срабатывает.
Причина названа в задаче — ещё не причина: 0030 пришла с «обернуть таблицу»,
и сделанное дословно экран не чинило.

**«Отчёты» и «Выгрузки» на телефоне тоже перестали уводить страницу вбок**
(589 → 390 и 541 → 390, задача 0031) — и причины у них разные, хотя задача
называла общую. Две ловушки оттуда стоят того, чтобы знать их из любого
модуля. Первая: **процентный предел не участвует в подсчёте min-content**,
поэтому `max-width: 100%` у списка выбора не делает ничего — ряд всё равно
не может стать уже самого длинного пункта, и лечится это парой `min-width: 0`
плюс основа переноса в rem. Вторая: **у `fieldset` в таблице стилей браузера
стоит `min-inline-size: min-content`** — рамка отбора не обрезает свой
минимум, а растёт под него, унося вбок всю страницу; в нашем CSS этого
правила не видно, и искать его надо в браузере.

**Ряд полей на телефоне переносится.** Двенадцать полей формы колеса в один
ряд — это 761 пиксель при экране 386. Перенос, а не сжатие: у flex-элемента
`min-width` по умолчанию равен содержимому, и сжатие дало бы поля, в которые
не видно, что вводишь.

**И «Колёса» перестали уводить страницу вбок, когда склад назван длинно**
(529 → 390, задача 0041). Ломалось это только у клиента: ширина `<select>`
в Chrome равна самому длинному пункту, и «Основной склад на Ткацкой, бокс 3
(второй этаж)» просил 474 пикселя при 280 доступных, а на прогонных
«Ткацкой» и «Дальнем» экран укладывался. Отсюда общее правило про замер:
**длину строк в фикстуре задают данные клиента, а не удобство теста** —
длинное имя склада стоит теперь в общей фикстуре сторожа, и меряют его
все разделы разом.

Лечится это парой из 0031 (`min-width: 0` плюс основа переноса), и у неё
две обратные стороны, обе замеренные. Первая: на мониторе основа загоняет
кнопки карточки выгрузки на второй этаж, поэтому правило действует только
на телефоне. Вторая опаснее — **уезд вбок лечится и тем, что поле сжимают
до нечитаемого**, а проверка ширины страницы такую «починку» принимает:
`min-width: 0` в одиночку даёт 84 пикселя на поле при зелёных 23 разделах.
Правило общее: у правки раскладки два края, и проверять надо оба.

**Проверяется это узким viewport'ом, а не уменьшением окна.** Окно браузера
ниже некоторой ширины не сжимается, и медиазапросы не срабатывают —
`(max-width: 899px)` остаётся выключенным, то есть проверка молчит. Рабочий
приём: временный `iframe` шириной 390 на той же странице; у него свой
viewport, и правила включаются по-настоящему.

**Фронтенд.** Приёмка с телефона и рабочее место продавца: поиск по свободному
остатку, корзина, клиент, оформление с резервом, выдача, оплата, отмена,
возврат документом и перенос позиций. До чужой сделки продавец добирается
через клиента: возвращают через неделю, номера документа приезжающий
не помнит. Плюс разбор нераспознанных наименований — число на вкладке
единственное, что о них сообщает: сам список владелец не откроет, пока
не узнает, что там сотня после импорта.

Корзина при этом **видна всегда**: число позиций и сумма стоят в шапке экрана
продажи и не уезжают с ней, нажатие уводит к оформлению одним движением,
пустая корзина словами говорит, что делать. До этого блок «В сделку» лежал
под находками — «фара» на живом складе это 180 позиций и 2650 пикселей
прокрутки туда и обратно за каждой следующей деталью, а сколько набрано
и на сколько, продавец по дороге не знал вовсе.

**Сумма к оплате подставлена долгом.** В карточке сделки поле «принять
оплату» открывается с остатком долга (`сумма − оплачено`), и полная оплата —
самый частый случай на разборке — делается одним нажатием; после частичной
оплаты в поле встаёт новый остаток, а не сумма сделки. Частичная оплата
работает как раньше: подставленное стирают и вписывают своё. Кнопка «Оплата»
при этом перестала молчать — погашенная, она называет причину (пусто, ноль,
долг закрыт, сделка закрыта), а сумма больше долга предупреждает о лицевом
счёте **до** нажатия. Само поведение переплаты не менялось.

**Срок резерва виден продавцу и продлевается им же.** В карточке сделки под
номером стоит «Отложено до 8 сентября», в строке списка сделок клиента — то же
число рядом со статусом, а у просроченной на обоих местах красное «срок истёк»
вместо вчерашней даты. Продлить можно оттуда же, выбором даты; продление
пишется в историю документа с автором. Число лежало в базе с самого начала,
но на экране его не было ни в каком виде: продавец не знал, освободится деталь
завтра или через неделю. Резерв при этом по-прежнему не снимается сам —
поведение не менялось, задача была про показ.

**Разбор недоставленного.** Непринятое обработчиком событие повторяет робот
с растущей выдержкой, после пяти попыток отдаёт человеку на экран «Доставка»
(`/api/events/dead-letters`, владелец и менеджер). Оттуда его повторяют
по кнопке или снимают без отправки.

**Перевозка между складами — пачкой, а не по одной детали.** На витрине
склада отмеченные строки перевозятся одним документом (роли — владелец,
менеджер, кладовщик): количество не спрашивается, едет весь остаток позиции
на складе-источнике. Позиция, часть остатка которой отложена под клиента,
в документ не попадает — остальные всё равно уезжают одной перевозкой,
а форма показывает, кто остался и почему. Отмеченные с разных складов
перевезти нельзя: «Откуда» подставляется, только если он общий у всех,
иначе форма просит выбрать склад и везёт с него одного, остальные остаются
на месте. Частичная перевозка («две из пяти») по-прежнему делается из
карточки позиции.

**И журнал перевозок.** Вкладка «Перевозки» показывает документы перемещения
свежими сверху — дата, номер, откуда, куда, число позиций, примечание,
автор, — а нажатие на строку раскрывает её состав со ссылкой в карточку
каждой позиции. До этого движение между складами было видно только
построчно, в истории одной позиции; ответить «что мы увезли на второй склад
в августе» было нельзя.

**Заказы с площадок.** Заказ, оформленный покупателем на Дроме или Авито,
заводится сделкой с номером заказа площадки (`POST /api/deals/orders`,
продавец): товар резервируется сразу, повтор по тому же номеру возвращает
прежнюю сделку, необеспеченный заказ записывается черновиком с объяснением.
Вкладка «Заказы» показывает те, по которым ещё не ответили, с остатком времени
до срока площадки. Приём пока ручной: ключ к API защищённых сделок Дром выдаёт
по запросу.

**Переезд с предыдущей системы.** Выгрузка машин и выгрузка товаров грузятся
двумя файлами (`POST /api/import/bazon`, только владелец), колёса — третьим
(`POST /api/import/bazon/wheels`, прайс площадки: в выгрузке товаров шинных
полей нет вовсе). Вместе с ними переезжают поставки, доноры, резервы
по складам и флаг «Выгружать»; фотографии тянутся отдельным проходом,
пачками по двести. Повтор безопасен и дозаполняет пустые поля.
Замерено на живом файле: 35 841 позиция и 440 машин за 68 секунд.

**Метрик мало написать — их надо отдать.** `micrometer-registry-prometheus`
в зависимостях не было, и `/actuator/prometheus` не заводился вовсе, хотя
в `management.endpoints` он перечислен: конфигурация выглядела рабочей,
а адрес отвечал 401, как любой несуществующий. Тесты этого не видели — они
берут `SimpleMeterRegistry` и до отдачи наружу не доходят; поймал живой прогон.
Стережёт теперь `MetricsEndpointTest`. Сборщику адрес открыт без учётной
записи (у процесса её нет и быть не может), а наружу закрыт дважды: порт
приложения не опубликован, и терминатор отдаёт на `/actuator` 404.

**Метрики очереди событий.** `partsflow.outbox.pending`,
`partsflow.outbox.oldest.seconds`, `partsflow.deadletter.unresolved`
и `partsflow.deadletter.attention` — по ячейке целиком, не по арендатору:
пятьсот временных рядов на каждое число дадут хранилище метрик размером
с базу. Глубина без возраста не значит ничего: сто событий, появившихся
секунду назад, — обычная работа; те же сто возрастом в час — остановившийся
релей. Ждущее человека считается отдельно от того, что повторяет робот.

**Развёртывание закрыто.** Ячейка поднимается целиком: терминатор
с автоматическими сертификатами, приложение и хранилище без публикации портов,
управляющий контур закрыт по адресам, бэкапы с проверкой восстановлением,
накат миграций шагом деплоя.

**Наблюдение замкнуто до адресата.** `/actuator/prometheus` отдаёт глубину
outbox, возраст самого старого неотправленного события и разбор отказов,
а рядом в ячейке стоят три контейнера: `prometheus` собирает,
`alertmanager` шлёт в Telegram и повторяет, пока не починили, `pushgateway`
принимает отметки от бэкапа. Тревогу вешать на возраст, а не на глубину. Kafka поднимается профилем compose
(`--profile kafka`) и проверена живым прогоном на двух экземплярах: 40 событий,
ноль дубликатов.

**Отставшие схемы приложение называет при старте.** Если код рассчитывает
на миграцию, которой у арендаторов нет, в логе стоит `WARN` со счётом
отставших, первыми именами схем, ожидаемой версией и командой наката —
раньше приложение поднималось молча, а первым про отставание узнавала
площадка, забравшая пустой прайс. Идёт по отметке `schema_version` в реестре,
запуск не валит: разработчик работает как раз между накатами.

**Нет вовсе.** Protobuf-контрактов и Schema Registry — отложены осознанно,
причина в `docs/deployment.md`. Фискализации 54-ФЗ — и это решение,
а не забытая задача: она начинается с договора с оператором и регистрации
кассы в налоговой, а не с кода. Что уже готово, чего не делать раньше
времени и что сдвинет дело — `docs/fiscal-54fz.md`. Главное оттуда:
не заводить `FiscalReceiptSender` без реализации, интерфейс раньше первого
договора повторит форму воображаемого оператора.

**Авито отложено решением владельца продукта.** Генератор фида и резолвер
маппинга в `publishing/avito` есть, но доводить их сейчас не нужно: ни маппинг
категорий, ни валидатор формата, ни отдача фида по URL. Выгрузка на первое
время — только Дром. Не предлагать Авито как следующую задачу.

**Пересчёт открывается одной ячейкой, а не только всем складом.** Форма
открытия получила поле «Ячейка» («Любая», конкретная или «Без адреса»)
со счётчиком «Найдено товаров: N»; документ на десятки тысяч позиций
никто не закрывает, а полка — это минуты. Рядом со «Сканировать ячейку»
встала «Сканировать деталь»: каждый скан прибавляет к факту единицу, а код,
которого нет в текущей выборке, но который есть на складе, уходит в группу
«С проблемами» со статусом «Вне списка» — не теряется, а ждёт разбора.
Список делится вкладками `Все · Не сканировались · С проблемами ·
Отсканированы`. Сведение расхождений и проведение не менялись.

**Владелец видит список всех пересчётов, а не ищет открытый по складу.**
Вкладка «Пересчёт» получила журнал документов над обходом и сведением
расхождений: воронка «В работе · Выполненные · Отменённые · Все
пересчёты», колонки «Номер/дата · Выборка · Статус · Посчитано ·
Комментарий», подвал «Пересчётов: N». «Выполненные» — это и завершённый
подсчёт, и проведённый: нормально закрытый пересчёт всегда проведён.
Нажатие на строку открывает сведения любого пересчёта, а не только
открытого, — до этого закрытый документ был не найти вовсе, хотя журнал
склада на него ссылается («Пересчёт №4»). Открыт владельцу, менеджеру
и роли «Просмотр»; сводит расхождения по-прежнему только первые два.
Отдаётся страницей в двести строк, и подвал считает воронку целиком,
а не показанное: обрезка названа отдельной строкой.

**И у пересчёта появился комментарий** — свободный текст, который
и есть то, ради чего в такой журнал заходят: «83619 не найден»,
«Не сканировали», «Катушки не считали». Номер с датой говорят, что
документ был; зачем его открывали, не говорил никто. Пишут его владелец
и менеджер (`POST /api/inventory/sessions/{id}/note`), пока пересчёт
не проведён и не отменён, — у закрытого он показан текстом. Пустой
комментарий стирается в `NULL`, а не в пустую строку.

**Кладовщик его пока не пишет, и это ждёт владельца продукта.** Задача
называла его дословно — комментарий знает тот, кто ходил по полкам, —
но где полю стоять на экране обхода с телефона, она не описывает,
а экран этот офлайновый. Права у кладовщика поэтому нет: разрешение
без места, откуда им воспользоваться, читается следующим как готовый
путь. Вернётся оно вместе с экраном, одной веткой.

**Раздел «Сделки»: продавец видит их списком, а не ищет по клиенту.**
Экран «Сделки» (владелец, менеджер, продавец) и `GET /api/deals/registry`
показывают документы воронкой «Отложенные · Выданные · Отменённые · Все»,
открываясь на отложенных — это то, что требует действия. В строке номер
с датой, клиент, сумма, оплачено, состояние со сроком резерва (просроченный
— красным «срок истёк», а не вчерашним числом) и ответственный; подвал
называет, сколько показано из скольких. Отбор «Все продавцы / Мои» —
продавец чаще ищет своё, но чужое ему тоже нужно: возвращают не тому,
кто продавал. Нажатие на строку открывает сделку на «Продаже».

**И главное — поиск по номеру детали, которого не было вовсе.** До этого
к чужой сделке вела ровно одна дорога, «Найти сделку клиента», и она
спрашивала **только клиента**: «я вчера у вас откладывал, фамилия Петров»
работало, если фамилия записана так же, как её сейчас произнесли, а «мне
звонили, деталь номер такой-то» не работало никак — пути от товара
к сделке в системе не существовало. Теперь поиск идёт по номеру сделки
(точно), по клиенту и по публичному коду детали (вхождением), тремя
ветками через `UNION` — каждая своим индексом. Замерено на складе
в 35 841 позицию и 12 000 сделок: поиск по коду детали 1,2 мс,
воронка 0,6 мс. Себестоимости и наценки список не отдаёт — это отчёты
владельца.

**Продавец сужает найденное отбором, а не переписыванием запроса.** Под
полем поиска стоит ряд отбора — марка, модель, год выпуска (от–до), сторона,
перед/зад, склад, состояние и цена (от–до), — плюс сортировка по цене
в обе стороны и по дате приёмки. Отбор уходит **в запрос к базе**, а не
сужает показанные строки: «фара» на живом складе это 181 позиция при
пятидесяти показанных, и ниссановская фара срезается пределом до того, как
продавец успел назвать марку. Счётчик после отбора говорит «Найдено 6»
вместо «первые 50 из 181», пустая выдача объясняется словами, а весь отбор
снимается одной кнопкой. Марки, модели и состояния предлагаются те, что
встретились в найденном, — и не сужаются уже поставленным отбором, иначе
с Toyota нельзя было бы переключиться на Nissan.

**Раздел «Платежи» — касса компании построчно.** Владелец и менеджер
открывают экран «Платежи» (`GET /api/payments`) и видят все деньги
компании одним списком: дата, клиент, по какой сделке, чем платили,
комментарий и две колонки — «Приход» и «Расход». Воронка слева —
«Все · Приходные · Расходные», сверху период. Подвал считает **по всей
выборке отбора**, а не по показанной странице: «Платежей: 3 · приход
6 000 ₽ · расход 5 000 ₽ · итого 1 000 ₽», и обрезка названа отдельной
строкой. Суммы воронок складываются в итог «Всех» — это и есть проверка
того, что знак у расхода не разъехался.

До этого платёж было видно только внутри своей сделки и в карточке
знакомого клиента: источник система пишет с задачи 0024, а «сколько
за неделю прошло через кассу и что это за расход в четверг» спросить
было негде — отчёт «Платежи по источникам» отвечает суммами за месяц.
Нажатие на номер сделки открывает её на «Продаже», как в реестре
возвратов. Экран только читает: принять оплату, вернуть деньги
и пополнить счёт по-прежнему можно там же, где и раньше. Фискализацией
54-ФЗ это не является и её не приближает — реестр показывает то, что уже
записано.

**Раздел «Клиенты».** Владелец, менеджер и продавец находят клиента
по имени, телефону или почте в общем списке с балансом (экран «Клиенты»)
и открывают карточку с четырьмя вкладками — «Клиент · Сделки · Возвраты ·
Платежи». До этого клиент существовал только как поиск в разговоре
(`CustomerPicker`) и как «Найти сделку клиента» (`DealFinder`): список
клиентов не открывался ниоткуда, а поправить телефон, записанный
с ошибкой, было негде — при том что схема держала `email`, `customer_type`,
`inn`, `company_name`, `note` и `public_note` с самого начала.

Вкладка «Клиент» правит эти поля (владелец и менеджер; продавец видит
карточку текстом), включая тумблер «Данные юридического лица», и показывает
лицевой счёт с кнопкой «Пополнить / Списать» — той же операцией, что
«Положить»/«Выдать» в `DealFinder`, только из другого места входа. «Сделки»
и «Возвраты» переиспользуют существующие эндпоинты (второй — реестр
задачи 0021, отфильтрованный по клиенту); «Платежи» показывает и кассовые
платежи, и журнал лицевого счёта **целиком, постранично**, а не восемь
последних записей, как экран продавца. Пополнение и выдача со счёта
спрашивают источник платежа — тот же список и то же умолчание, что
у продавца: иначе половина принятых денег осталась бы в журнале без
способа, и «сколько прошло наличными» перестало бы сходиться.

Заодно **история сделок клиента закрыта ролями**: `GET /api/deals?customerId=`
и `GET /api/deals/{id}` были открыты любому вошедшему, то есть кладовщик
и «Просмотр» видели суммы и долг покупателя, хотя раздел «Клиенты» им
не показан. Сделку теперь читает тот, кто может по ней действовать;
поиск товара с ценой и наличием открыт по-прежнему всем.

**А с ней и её разделы (задача 0029).** Дверь тогда закрыли, а окна той же
комнаты остались открыты: возвраты сделки, её журнал (`message` и автор)
и список просроченных резервов читал любой вошедший — «Просмотр», получив
403 на карточку сделки, доставал по тому же идентификатору её возвраты
и журнал. Теперь разделы одного документа идут за самим документом
(и кладовщик, выдающий по нему товар, их видит), а выборка чужих сделок
пачкой — за историей покупателя. Справочники источников и услуг открыты
всем по-прежнему: экран «Продажа» положен каждой роли, а сделок по ним
не видно — и в коде у каждого читающего пути теперь либо стоит проверка,
либо написано, почему её нет.

**«Скачать таблицу» — владельцу и менеджеру, а не всякому вошедшему**
(задача 0049, решение владельца продукта от 9 сентября 2026: «чинить
сейчас», право тем же, кому открыты отчёты). Экраны «Склад» и «Шины
и диски» остаются открытыми всем — цену и наличие смотрит и кладовщик,
и «Просмотр», — но кнопка выгрузки уносила **весь склад одним файлом**:
номенклатура целиком, с ценами, поставками, заметками, кросс-номерами
и остатками по каждому складу. Себестоимости в файле нет и не было,
а всё остальное для разборки — опись имущества; первые десять клиентов
компании из одного города, и ушедший кладовщик с таким файлом уносит
готовый прайс конкурента. Закрыто с обеих сторон: `@PreAuthorize`
на `GET /api/parts/catalog/export` и `GET /api/wheels/export` плюс
скрытая кнопка (`EXPORT_ROLES` в `screens/tabs.ts`) — спрятанная кнопка
при открытом адресе не защита, а закрытый адрес при видимой кнопке
обещание, которое не выполняется. Состав файла не менялся ни на колонку:
задача про доступ.

**И это заплатка, названная заплаткой.** У ориентира выгрузка файлом —
самостоятельное полномочие, и даже два («Скачивать CSV из списков»
отдельно от списков сделок), то есть право, а не следствие доступа
к экрану. Когда задача 0044 заменит жёсткие роли полномочиями, эта
проверка станет полномочием «скачивать таблицу», и роли из неё уйдут;
записано здесь и в коде, чтобы не осталось двух способов решать одно.

**Журнал действий организации.** Экран «Журнал» и `GET /api/organization/audit`
показывают, кто что и когда поменял: строка называет человека, **его роль
на момент правки**, время, вещь названием и публичным кодом, что именно
изменилось и с чего на что («Иванов · Менеджер · Фара Toyota Camry · A7K3M2 ·
Цена: 5000 → 4500»). Заведение и удаление названы событием («Товар заведён»,
«Платёж записан»), а не перечислением полей. Отбор — по человеку и по полю
меню колонки (тот же `ColumnMenu`, что на витрине склада), по виду вещи
списком и по периоду; поиск идёт по названию, публичному коду и номеру
сделки. Следит журнал за карточкой товара, сделкой с позициями, платежом
и затратами по машине.

`audit_log` писался с самого начала и **не читался ниоткуда**: ни эндпоинта,
ни экрана — то есть журнал вели затем, чтобы однажды предъявить, и предъявить
его было нельзя. На «кто уронил цену» и «кто отменил сделку» отвечал
разработчик запросом в базу.

Экран прямо говорит, чего журнал не видит: правка, сделанная напрямую в базе
мимо приложения, в него не попадает. Это одна из трёх дыр, названных выше,
и экран её не закрывает — но и не делает вид, что её нет.

**Роль на момент действия пишется снимком.** Сотрудника переводят
из менеджеров в продавцы, и подтянутая при чтении роль сделала бы запись
«менеджер уронил цену» неправдой о человеке задним числом. Записи до
8 сентября 2026 роли не имеют, и на экране стоит прочерк — как и у автора,
которого не записали: подставленный автор выглядит ответом на вопрос «кто»,
будучи догадкой.

**И появилась шестая роль — «Ревизор».** Журнал открыт владельцу и ей, больше
никому: решение владельца продукта от 8 сентября 2026 о том, что доступ даёт
роль, а не именное разрешение (`tasks/0043-zhurnal-deystviy-organizatsii.md`).
Менеджера в списке нет намеренно — журнал заводят в том числе затем, чтобы
проверять распоряжающихся складом и деньгами. Роль ничего не меняет: списки
изменяющих методов перечисляют разрешённые роли, а не запрещённую.

**Журнала входов и выходов здесь нет** — вторая половина задачи 0043
(входы, выходы, устройство, длительность сессий) не сделана: вход не является
изменением строки, слушатель Hibernate его не видит, и таблицы под это
не существует. Она идёт **раньше** администратора сервиса (задача 0042):
именно ею клиенту показывают входы администратора.

Ближайшие задачи по плану (`docs/architecture-and-plan.md`, раздел 10):

1. **Пилот на двух клиентах** (`docs/architecture-and-plan.md`, недели 15–16).
   Порядок подключения расписан по шагам в `docs/onboarding.md` — от пустой
   ячейки до первой позиции, уехавшей на Дром, с замерами прогонного
   арендатора и с тем, что надо запросить у клиента и у Дрома заранее.
   Код закрывает сквозной путь целиком — от приёмки с телефона до выгрузки
   на Дром, — и следующее, что даст больше всего, это живой клиент, а не ещё
   одна функция. Разложить пилотных по разным ячейкам: десять компаний
   из одного города общаются между собой.

---

## Соглашения

Комментарии на русском и только там, где объясняют **почему**, а не что.
Названия классов и методов — на английском.

Тесты на бизнес-инварианты обязательны: изоляция арендаторов, идемпотентность
потребителей событий, неизменяемость журнала движений.

Миграции — только через Liquibase, никаких `ALTER` из кода.

Новая находка пишется в памятку того модуля, где живёт код. Сюда — только
если наступить на неё можно из любого модуля: доступ к схеме, транзакции,
коды ответа наружу, тесты. Иначе корневой файл прирастёт обратно, и его
снова перестанут читать целиком.

**Появилась возможность — опиши её там, где написано, что система умеет.**
Это раздел «Состояние и что делать дальше» выше и таблица §12
`docs/bazon-parity.md`. Памятка модуля отвечает на вопрос «почему так сделано
и чем заплачено» — она не заменяет ответа на вопрос «что система теперь может».
Из них решают, что делать дальше, и по ним переходящему клиенту рассказывают,
что он получит.

Проверено на себе: отметка о заборе прайса и наценка уехали в `main`, памятка
`publishing` про них знала, а корневой файл — нет. Читающий его узнал бы,
что выгрузки умеют ровно то же, что и месяц назад.
