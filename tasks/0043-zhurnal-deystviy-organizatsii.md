---
id: 0043
title: Владелец видит журнал действий своей организации — кто, что и когда
status: в работе
assignee: migrator
source: решение владельца продукта, 8 сентября 2026
pr: —
---

## Решение владельца — 8 сентября 2026

Дословно: «все действия внутри одной организации должны быть видны
владельцу и тем, кому владелец разрешил это».

## Ответы владельца — 8 сентября 2026

**Доступ даёт роль, а не именное разрешение.** Дословно: «роль». То есть
в нынешнем устройстве это шестое значение рядом с `OWNER`, `MANAGER`,
`STOREKEEPER`, `SELLER` и «Просмотром»; когда появятся полномочия
(задача 0044) — просто одно из них.

**Записи переводятся на язык экрана.** Дословно: «Иванов изменил цену
„Фара Toyota Camry“ с 5000 на 4500». Не «поле `price` у позиции 12345
стало 4500».

**И показывать надо больше, чем изменение.** Дословно: владелец должен
видеть «какая эта запчасть, какую роль имеет изменивший, когда это
произошло и вообще всё, что может хоть как-то пригодиться владельцу».

Отсюда состав строки: **кто** (имя и его роль на момент действия),
**когда**, **что за вещь** (позиция названием и публичным кодом, а не
идентификатором), **что именно изменилось** и **с чего на что**.

Роль **на момент действия** — это не придирка: сотрудника переводят
из продавцов в менеджеры, и запись «менеджер уронил цену» станет ложью
задним числом, если роль подтягивать текущую.

**Нужны выборки и отбор.** Дословно: «должна быть возможность делать
выборки событий и фильтровать их». По человеку, по виду события,
по периоду, по вещи — тем же механизмом отбора колонок, что уже стоит
на витрине склада и на вкладке колёс (`ColumnMenu`), а не своим:
второй способ отбирать разойдётся с первым.

**И входы тоже — с устройством и длительностью.** Дословно: «владелец
мог видеть входы, выходы, с какого устройства, длительности сессий
и тд и тп».

### Это отдельная работа, и её сейчас нет вовсе

`audit_log` пишет **изменения сущностей** через слушателя Hibernate.
Входов, выходов, устройств и длительности сессий он не пишет и писать
не может: вход — это не изменение строки в базе. Ни таблицы, ни записи
об этом сегодня не существует.

Значит задача делится надвое, и вторая половина крупнее, чем кажется:

1. **Журнал изменений** — читаемый показ того, что уже пишется.
2. **Журнал сессий** — новое: вход, выход, устройство, длительность.
   Заводится с нуля, требует миграции и решения о том, что считать
   устройством (строка `User-Agent`? разобранная «Chrome на Windows»?)
   и что делать с сессиями, которые не кончились выходом, а истекли.

Вторая половина — это ещё и то, чем показывают клиенту входы
администратора (задача 0042). То есть 0043 в части сессий идёт **раньше**
администратора, а не рядом с ним.

## Что человек сможет

Владелец открывает журнал и видит, кто что сделал в его разборке: кто
уронил цену, кто отменил сделку, кто списал деталь, кто заходил
в кабинет. И может открыть этот журнал не только себе — но лишь тому,
кому сам разрешил.

## Почему это важно

**Журнал пишется с самого начала и не читается ниоткуда.** Проверено
8 сентября 2026: `AuditLogListener` (слушатель Hibernate) пишет
`audit_log` на каждое изменение сущности, а наружу его не отдаёт
**ни один эндпоинт** — `grep` по контроллерам не находит ничего,
экрана нет тоже.

Это ровно та болезнь, которая в корневом `CLAUDE.md` названа отдельно
и стоила проекту восьми случаев: возможность написана, покрыта тестами
и человеку недоступна. Только здесь она хуже обычной — журнал ведут
именно затем, чтобы однажды предъявить, и до этого дня никто не знает,
что предъявить его нельзя.

**И без этого экрана нельзя выполнить два других решения владельца
от того же дня:** «пиши настоящего автора в журнал» и «показывай клиенту
входы» (задача 0042, администратор сервиса). Писать есть куда, показывать
негде. Поэтому 0043 идёт **раньше** работы над администратором,
а не после.

**Цена ошибки — не в удобстве.** «Кто уронил цену» спрашивают ровно
тогда, когда подозревают правку мимо интерфейса, а «кто отменил сделку» —
когда клиент приехал за деталью, которой нет. Сегодня на оба вопроса
отвечает разработчик запросом в базу, то есть клиент ждёт человека,
чтобы узнать то, что система знает.

## Что решает владелец — до работы

**«Тем, кому владелец разрешил» — это новая механика прав.** Сегодня
права даёт роль, и только она: `OWNER`, `MANAGER`, `STOREKEEPER`,
`SELLER`, «Просмотр». Разрешения, выдаваемого одному человеку поимённо,
в системе нет вовсе.

Отсюда вопрос, который нельзя вывести из ответа: **это новая роль
(«кто видит журнал») или именное разрешение конкретному участнику?**
Роль дешевле и ложится на существующее устройство; именное разрешение
ближе к сказанному, но заводит второй способ раздавать права рядом
с первым — а два способа расходятся молча, это в проекте уже случалось
с белыми списками колонок и с копиями словарей.

Второе: **что именно показывать**. Журнал сущностей — это записи вида
«поле `price` у позиции 12345 стало 4500», и человеку такое читать
тяжело. Нужен ли перевод на язык экрана («Иванов изменил цену
„Фара Toyota Camry“ с 5000 на 4500»), и по каким событиям — по всем
или по избранным?

## Что известно к работе

- Пишет журнал `AuditLogListener`, слушатель Hibernate. Он видит **только
  то, что прошло через Hibernate**: движение, записанное прямым SQL мимо
  `StockLedger`, в журнал не попадёт. Это записано в корневом `CLAUDE.md`
  как одна из трёх дыр, и экран журнала эту дыру не закрывает —
  но и не должен делать вид, что её нет.
- В бою журналы принадлежат владельцу схем, а рабочая роль имеет на них
  только `SELECT` и `INSERT`: переписать журнал приложение не может,
  и это проверено попыткой. Значит показанному можно верить настолько,
  насколько верен доступ к базе.
- Экран будет табличным и длинным — значит `.table-scroll`, обёртка
  и проверка сторожем из задачи 0032, иначе он войдёт в список экранов,
  уводящих страницу вбок, шестым.

## Как проверить, что сделано

Прогон живого сценария, а не только тест: изменить цену позиции одной
учётной записью, открыть журнал другой — увидеть автора, время, что
именно изменилось и на что.

**Доказательство откатом:** снять запись автора — журнал обязан показать
это пустотой или отказом, а не молча подставить текущего пользователя.
Подставленный автор здесь хуже отсутствующего: он выглядит как ответ
на вопрос «кто», будучи догадкой.

И обратная сторона обязательна: участник, которому владелец **не**
разрешал, журнала не видит — 403, закреплённый тестом. Снятие проверки
обязано давать `403 → 200`.

---

## Что сделано — 9 сентября 2026

**Сделана первая половина: журнал изменений.** Экран «Журнал»,
`GET /api/organization/audit` и `/values`, роль `AUDITOR` («Ревизор»),
changeset `tenant/062`. Состав строки — тот, что назван выше: кто, его
роль **на момент правки**, когда, вещь названием и публичным кодом, что
изменилось и с чего на что. Заведение и удаление названы событием.
Отбор — по человеку и по полю через `ColumnMenu` (тот же, что на витрине
склада), по виду вещи списком, по периоду и поиском по вещи.

**Вторая половина — журнал сессий — не сделана, и это сознательный
разрез, а не недоделка.** Причины две, и вторая важнее первой.

Первая: объём. Ни `SessionRegistry`, ни `HttpSessionEventPublisher`,
ни единого упоминания `User-Agent` или адреса клиента в проекте нет —
проверено перебором по `src/main`, `frontend/src` и `ops`. Выход
обрабатывает стандартный logout Spring без своего обработчика, срок
сессии — умолчание Tomcat, хранилище — память. То есть половина
заводится с нуля: таблица, запись на входе, запись на выходе, разбор
истёкших и разбор устройства.

Вторая: **задача сама называет здесь два решения, которых владелец
не принимал.** «Что считать устройством (строка `User-Agent`? разобранная
„Chrome на Windows“?) и что делать с сессиями, которые не кончились
выходом, а истекли». В разделе «Ответы владельца» на них ответа нет —
там сказано только «владелец мог видеть входы, выходы, с какого
устройства, длительности сессий и тд и тп». Решить их молча значит
принять продуктовое решение за владельца, а такие решения по договору
(`docs/agent-workflow.md`, «Кто и что может слить») ждут человека.

**Порядок при этом не нарушен и остаётся в силе:** журнал сессий идёт
**раньше** администратора сервиса (задача 0042) — именно им клиенту
показывают входы администратора. Сделанная половина ему не мешает
и даёт готовое место: экран, роль и механику доступа, к которым сессии
добавляются вкладкой.

### Что решено за владельца — ждёт подтверждения

1. **Роль названа «Ревизор» (`AUDITOR`).** Задача называет её «шестым
   значением», но слова на экране не даёт.
2. **Менеджеру журнал не показан.** Владелец сказал «владельцу и тем,
   кому владелец разрешил»; менеджер под это не подпадает, а журнал
   заводят в том числе затем, чтобы проверять распоряжающихся складом
   и деньгами. Если владелец имел в виду иное — это одна строка.
3. **Ревизор видит и денежные поля** (себестоимость, минимальную цену).
   В карточке позиции они отдаются только владельцу и менеджеру, но
   «кто уронил себестоимость» — из главных вопросов к журналу, и роль
   выдаётся владельцем поимённо.
4. **Отбор по роли не заведён.** Владелец назвал четыре среза — человек,
   вид события, период, вещь; роль показана колонкой, но не отбирается.
5. **Счёт в подвале обрезан двумя тысячами** («из более чем 2 000 —
   уточните отбор»): точный счёт на 141 955 записях переехавшего клиента
   стоит секунд на каждое открытие экрана.
