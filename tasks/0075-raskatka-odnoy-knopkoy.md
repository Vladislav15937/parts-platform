---
id: 0075
title: Раскатка одной кнопкой: выбор стенда, ветки, миграций и данных, старая сборка гаснет последней
status: слита
assignee: coder (agent-acf95d6f2586e41c5)
source: решение владельца продукта, 12 сентября 2026
pr: 214
---

## Решение владельца — 12 сентября 2026

Дословно: «сейчас для меня самое главное обеспечить непрерывную поставку
желательно одной кнопкой с возможностью выбора на какой стенд», «этот
стенд я должен уметь накатывать с выбором ветки, с выбором запускать
миграцию бд или нет и тд и тп», «я бы хотел иметь прозрачный инструмент
раскатки на стенды IFT ПСИ ПРОМ».

Решено там же: **Jenkins не заводим** — параметризованный запуск даёт
`workflow_dispatch`, а второй сервис пришлось бы держать правдивым
(плагины, секреты, обновления, бэкапы). Тестировщик получает доступ
в репозиторий и жмёт кнопку в браузере.

## Порядок выкладки — решение владельца от 12 сентября 2026

Дословно: «При деплоях должен быть следующий порядок. Прогоняется
миграция, база поднялась без проблем, ошибок нет, само приложение встало
штатно, всё в порядке и только тогда можно убирать старую сборку
и ставить новую и красить её в зелёный».

То есть **старая сборка обслуживает людей до тех пор, пока новая
не доказала, что поднялась.** Порядок шагов:

1. накат схем одноразовым контейнером новой версии (0073) — старая
   сборка продолжает работать на новой схеме, и это возможно только
   потому, что миграции здесь расширяющие;
2. поднять новую сборку **рядом** со старой, не подменяя её;
3. дождаться её готовности по healthcheck'у (0078) — не «порт отвечает»,
   а контекст поднялся, схемы на версии, журналы защищены;
4. переключить терминатор на новую (0079) — `caddy reload`, без разрыва
   соединений;
5. прогнать четыре самопроверки уже на переключённой;
6. и только теперь погасить старую и покрасить выкладку зелёным.

**Упавший шаг 3 или 5 возвращает трафик на старую сборку** — она ещё
работает, возврат мгновенный. Это и есть причина, по которой её
не гасят раньше.

**База обязана соответствовать версии артефакта — решение владельца
от 12 сентября 2026.** Дословно: «база также должна уметь возвращаться
в исходное состояние которое соответствует той или иной ветке. Не должно
быть такого чтобы версия приложения была 100 а база соответствовала
версии 103».

Отсюда меняется смысл шага 1: он не «накатить то, что не накатано»,
а **привести схему к версии артефакта — в обе стороны**. Откат сборки
на прежний SHA откатывает и схему до парной версии. Состояния «код 100,
база 103» не существует по построению, а не по внимательности
выкладывающего (0073 и 0081).

Чем откат вниз обеспечивается — зависит от стенда, и это не отговорка,
а разная цена: на ИФТ и ПСИ это `rollbackToTag`, на ПРОМ — возврат
парного слепка, снятого шагом самой выкладки. Причина в 0082: откат
структурно верный может потерять данные, и на живом клиенте это дороже
самой неудачной выкладки.

## Зависит от двух задач

**0073** (режим «накатить и выйти»), **0074** (образ собирается один
раз и публикуется), **0078** (готовность приложения машинно проверяема)
и **0079** (терминатор переключается на сборку). Без первой невыразима
галочка миграций, без второй по стендам поедет не тот артефакт, что
проверяли, без третьей нечем установить «встало штатно», без четвёртой
некуда переключать. Начинать после них.

## Форма

```
стенд:      ift | psi | prom
ветка/SHA:  любая для ift; для psi и prom — только SHA, зелёный в main
миграции:   накатить | не трогать | только миграции
данные:     не трогать | обезличенный слепок прома | эталонный набор
```

**«Зелёный в main» проверяется, а не доверяется.** Человек под давлением
выберет ветку и на ПСИ; отказ словами дешевле разбора того, что уехало
на приёмку.

**ПРОМ — за обязательным подтверждением владельца** (GitHub Environments,
required reviewer). Исполнитель этой задачи на ПРОМ не выкладывает
ни разу: там живые данные клиента.

## Прозрачность — это запись, а не кнопка

Каждая выкладка обязана оставлять след, по которому можно ответить
«что сейчас на ПСИ» без расспросов: стенд, SHA, ветка, кто нажал,
накатывались ли схемы, какая версия схемы получилась, результат
самопроверок. Страница Environments для этого и существует.

## Четыре самопроверки, без которых это не выкладка

Сегодня их делают глазами (`docs/pilot-server.md`, «Повседневные
команды»):

1. `curl /` отвечает 200;
2. в логе `Журналы защищены`, а не «правятся прямым SQL» — иначе ячейка
   работает одной ролью и журналы перестали быть доказательством;
3. отставших схем ноль;
4. число `<offer>` в прайсе не упало до нуля — пустой прайс площадка
   читает как «снять всё», и это худший исход выкладки.

Упавшая самопроверка обязана красить выкладку. Молча зелёный деплой,
после которого прайс пуст, хуже упавшего.

## Решение владельца о смысле отката — 14 сентября 2026, позднее

Дословно — «Да, согласен» на предложение сессии, которое записано ниже.
Оно **уточняет** прежнее правило «база соответствует сборке», а не отменяет
его, и уточнение взято из того, как это делают банки и Amazon.

**Откатывается только приложение. Базу при откате не откатывают — её
чинят вперёд.** Причина одна: откат схемы уносит то, что люди записали
после выкладки. Даже у эталона (blue/green в Amazon RDS) кнопки отката
после переключения нет: старая база — снимок на момент переключения,
записанное после живёт только в новой.

**Правило парности в новой формулировке: база не соответствует версии,
а совместима с ней. База может быть новее приложения, но никогда не старее.**

- приложение 100 на базе 103 — **нормальное состояние после отката**,
  при условии что миграции 101–103 совместимы с кодом 100;
- приложение 103 на базе 100 — **запрещено**: новый код ищет то, чего нет.
  Это готовность уже держит несимметрично (0078).

Совместимость держится дисциплиной expand/contract: миграция только
добавляет; переименование, смена типа, обязательное поле и удаление
растягиваются на несколько релизов, и удаляется старое не раньше, чем
к версии, которой оно было нужно, точно не вернутся. Сторож этого — 0116.

**Что это значит для механики:**

- **копия базы при выкладке (0112) остаётся** — она даёт «всё или ничего»
  **до переключения**: не встала новая сборка — копия удаляется, старая
  база не тронута;
- **откат после переключения — это выкладка прежнего образа без изменения
  схемы.** Не миграция вниз и не возврат к старой копии;
- **миграция вниз и возврат слепка остаются инструментами стендов и аварий**
  (0081, 0082, 0102), а не частью обычного отката. Цену они по-прежнему
  обязаны печатать до нажатия.

Отсюда же ответ на второй вопрос дирижёра от 14 сентября («опускать схему
до переключения трафика или после»): **при обычном откате схему не опускают
вовсе** — трафик переводится на прежнюю сборку, и всё.

## Решение владельца от 14 сентября 2026 меняет механику отката

**База едет вместе со сборкой** — задача **0112**, там решение записано
дословно. Коротко: при выкладке поднимается копия базы под новую сборку,
старая работает на своей нетронутой, переключение трафика и базы —
одно событие; не получилось — копия удаляется, старая база не менялась.

Для кнопки это значит:

- **шаг «привести схему» на боевой ячейке превращается в «поднять копию
  и накатить на неё»**; приведение вниз остаётся механикой стендов;
- **откат перестаёт быть миграцией вниз** — он становится «остаться
  на старой базе», то есть бесплатным и мгновенным;
- **кнопка обязана уметь оба режима**: на стендах приведение схемы
  на месте (там копия не нужна и данные не жалко), на ПРОМ — копия.

Раздел ниже про образ с телами откатов **остаётся верным для стендов**
и для случая, когда копию не делают; на ПРОМ он перестаёт быть путём
отката.

Единственное, что делать нельзя, — **считать задачу закрытой, пока
не решён вопрос записи между копией и переключением** (0112, раздел
о заморозке и репликации). Кнопка, теряющая смену продаж, хуже
отсутствующей.

## Откат вниз делает образ, у которого есть тела откатов

Установлено на волне 14 сентября 2026, при закрытии 0073, и это
**меняет порядок шага, а не отменяет требование владельца**.

Образ версии 100, встретив схему 103, опустить её не может физически:
тела `--rollback` для changeset'ов 101–103 лежат в чужом jar, а
`rollbackCount` их не видит и снял бы вместо них последние **свои**.
Поэтому 0073 такую схему не трогает, называет по имени и возвращает
ненулевой код — правильное поведение, но само по себе оно оставляет
требование «не должно быть кода 100 при базе 103» невыполненным.

**Выполняется оно порядком шагов.** Откат с 103 на 100 — это:

1. одноразовый контейнер **той сборки, что сейчас развёрнута** (103),
   приводит схему к версии 100 — у неё есть тела откатов 101–103;
2. и только потом трафик переводится на образ 100.

То есть кнопка обязана знать **обе** версии — куда откатываемся
и что сейчас стоит, — и мигрировать **старшим** образом, а разворачивать
младший. Сегодня шаг миграции знает только целевую.

**Цену отката при этом считает `db/rollback-cost.py`** (0082) и печатает
до нажатия: что потеряется в данных. Порядок шагов не отменяет этого,
а предшествует ему.

## Откат


Тот же деплой с прежним SHA. **И честная оговорка в документе:** код
откатывается одной командой, применённая миграция — нет. Безопасным
откат делает только дисциплина расширяющих миграций и неизменяемость
changeset'ов; при кнопке её надо держать жёстче, а не слабее.

## Что известно к работе

- `workflow_dispatch` умеет `type: choice` и `type: boolean` — форма
  с выпадающими списками рисуется сама, терминал тестировщику не нужен.
- Секреты — только в GitHub Environments. Репозиторий публичный.
- Стенды: ИФТ и ПСИ — **на разных машинах** (решение владельца от
  12 сентября); правило «одна ячейка на сервер» остаётся.
- Машины, домены и секреты стендов заводит владелец — это не работа
  исполнителя. Задача делает инструмент, который на них нацеливается.

## Как проверить, что сделано

Живой прогон на ИФТ: выложить ветку, выложить её же без миграций,
накатить только схемы, вернуться на прежний SHA. Все четыре комбинации
обязаны работать и оставлять запись.

**Доказательство откатом на самопроверках** — главное здесь: сломать
каждую из четырёх по очереди (погасить приложение, снять разделение
ролей, оставить схему отставшей, отдать пустой прайс) и убедиться,
что выкладка краснеет. Проверка, которая не краснеет на дефекте, хуже
отсутствующей — это в проекте уже записано дважды.

## Сделано PR #214 — кнопка в нынешней механике, стенды

14 сентября 2026. Разведение на две ветки — решение дирижёра той же волны:
кнопка сейчас, копия базы (0112) следом.

**Закрыто.** Форма `workflow_dispatch` со всеми четырьмя полями
(`.github/workflows/deploy.yml`); порядок выкладки целиком
(`ops/deploy.sh`) — схема, новая сборка рядом, готовность, переключение,
самопроверки, и только потом старая гаснет; четыре самопроверки
(`ops/deploy-checks.sh`), каждая краснеет на своём дефекте и называет себя
по имени; план по **двум** версиям (`tools/deploy-plan.py`) — «мигрируем
старшим образом, разворачиваем младший», то есть требование раздела
«Откат вниз делает образ, у которого есть тела откатов» выполнено;
проверка «зелёный в main» для ПСИ и ПРОМ; ПРОМ за подтверждением владельца
(GitHub Environments); запись о каждой выкладке в трёх местах.
Что человек видит — `docs/deploy-button.md`.

**Не закрыто и остаётся за 0112:** режим «копия базы» на ПРОМ и вопрос
записи между снимком и переключением. До 0112 ПРОМ идёт тем же путём,
что и стенды, и это сказано вслух в документе, в коде workflow
и в `ops/CLAUDE.md`.

**Два вопроса владельцу продукта** (в теле PR #214 отдельным разделом):
чем закрывать два значения поля «Данные», которых в системе нет (сейчас
они честно останавливают выкладку словами), и допустимо ли опускать схему
**после** переключения трафика, чтобы убрать окно при выкладке вниз.

**Живого прогона на ИФТ не было — машины ещё нет.** Прогон был местный,
на боевом compose: все четыре сочетания задачи плюс неудачная выкладка
с возвратом трафика. Что проверено настоящим прогоном, а что нет —
в теле PR и в `ops/CLAUDE.md`.
